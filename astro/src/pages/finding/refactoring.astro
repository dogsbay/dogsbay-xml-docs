---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: finding/refactoring.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Step from "@ui/steps/Step.astro";
import Steps from "@ui/steps/Steps.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"refactoring","text":"Refactoring"},{"depth":2,"slug":"the-rule-that-makes-them-safe","text":"The rule that makes them safe"},{"depth":2,"slug":"before-you-change-anything","text":"Before you change anything"},{"depth":2,"slug":"what-each-refactoring-is-for","text":"What each refactoring is for"},{"depth":3,"slug":"moving-and-renaming","text":"Moving and renaming"},{"depth":3,"slug":"changing-how-content-is-referenced","text":"Changing how content is referenced"},{"depth":3,"slug":"restructuring","text":"Restructuring"},{"depth":2,"slug":"a-worked-example","text":"A worked example"},{"depth":2,"slug":"when-a-refactoring-refuses","text":"When a refactoring refuses"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/finding/refactoring";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Refactoring";
const description = "Rename, move, split, and restructure content without breaking its references." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "how-to";
const pageAudience = null;
const pageCategory = ["finding"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Refactoring {#refactoring}\n\nFiles in a documentation set depend on one another. When you rename a file, key, or element ID, references in other files must change too. The editor's refactoring commands update those references.\n\n## The rule that makes them safe {#the-rule-that-makes-them-safe}\n\nEvery refactoring prints a plan and changes nothing. You read the plan, then run the same command again with `--apply`.\n\n```bash\ndogsbay-xml rename-key product-name product --root .\ndogsbay-xml rename-key product-name product --root . --apply\n```\n\nIn the editor the same plan appears as a table you review before it runs.\n\n> [!NOTE]\n> Two commands work the other way round. `edit-map` and `edit-reltable` write unless you pass `--dry-run`, and so does `metadata-set`. They are editors rather than refactorings.\n\n## Before you change anything {#before-you-change-anything}\n\nAsk what depends on it:\n\n```bash\ndogsbay-xml where-used topics/installing-audacity.dita \\\n  --root . --map audacity-guide.ditamap\n```\n\nWith `--map`, the report includes references that reach the file indirectly through a key, which are the ones a search would miss.\n\n## What each refactoring is for {#what-each-refactoring-is-for}\n\n### Moving and renaming {#moving-and-renaming}\n\n| Command | Use it when |\n| --- | --- |\n| `rename-file` | A file's name or location is wrong. |\n| `rename-key` | A key's name is wrong, or you are aligning a naming scheme. |\n| `rename-element-id` | An element ID is wrong and conrefs point at it. |\n| `rename-profile-value` | A condition value changes, such as `mac` becoming `macos`. |\n| `delete-file` | A file should go. It reports inbound references first, so you find out before rather than after. |\n| `retarget` | Two files should become one: point every reference from one at the other. |\n\n### Changing how content is referenced {#changing-how-content-is-referenced}\n\n| Command | Use it when |\n| --- | --- |\n| `keyify` | A file is referenced by path in many places and should be a key. |\n| `inline-key` | A key is not earning its indirection. |\n| `extract-conref` | The same content is repeated and should be reused from one place. |\n| `inline-conref` | Reused content should become a local copy again. |\n| `create-keydef` | You want a text key, such as a product name, defined once. |\n| `merge-keydefs` | A map's closure defines the same key more than once and only the first can win. |\n\n### Restructuring {#restructuring}\n\n| Command | Use it when |\n| --- | --- |\n| `split-topic` | A topic has grown into several. Each top-level section becomes a topic, and the map is updated to include them. |\n\n## A worked example {#a-worked-example}\n\nThis example turns a hardcoded product name into a key. It fixes one of the problems in the sample project.\n\n:::steps\n1. **Define the key** The key name and the text it resolves to are the two arguments; the map that receives the definition is an option.\n\n   ```bash\n   dogsbay-xml create-keydef product-name Audacity \\\n     --map keydefs-product.ditamap --apply\n   ```\n2. **Replace the hardcoded text as well** `--replace-in` rewrites whole-word occurrences of the text under a root into key references, which is the part that would otherwise be a hundred manual edits.\n\n   ```bash\n   dogsbay-xml create-keydef product-name Audacity \\\n     --map keydefs-product.ditamap --replace-in topics\n   ```\n3. **Apply it** Run the same command with `--apply` once the plan looks right.\n4. **Check the result**\n\n   ```bash\n   dogsbay-xml project-health . --map audacity-guide.ditamap\n   ```\n:::\n\n## When a refactoring refuses {#when-a-refactoring-refuses}\n\nIf a refactoring would create a broken reference, it reports the problem instead of making the change. Review the plan to find unsafe changes before they affect the output.\n\n## Related {#related}\n\n:::cards\n- **[Validating a project](./validation){icon=\"check\"}**\n  Find what needs fixing, and confirm the result.\n\n- **[Keys and reuse](/dogsbay-xml-docs/authoring/keys-and-reuse){icon=\"key\"}**\n  The mechanisms these refactorings move between.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/finding/refactoring.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  pagefindBaseUrl={"/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="refactoring" class="group scroll-mt-20">Refactoring<a href="#refactoring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h1>
      <p>Files in a documentation set depend on one another. When you rename a file, key, or element ID, references in other files must change too. The editor's refactoring commands update those references.</p>
      <h2 id="the-rule-that-makes-them-safe" class="group scroll-mt-20">The rule that makes them safe<a href="#the-rule-that-makes-them-safe" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>Every refactoring prints a plan and changes nothing. You read the plan, then run the same command again with <code>--apply</code>.</p>
      <CodeBlock code={"dogsbay-xml rename-key product-name product --root .\ndogsbay-xml rename-key product-name product --root . --apply"} lang="bash" />
      <p>In the editor the same plan appears as a table you review before it runs.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>Two commands work the other way round. <code>edit-map</code> and <code>edit-reltable</code> write unless you pass <code>--dry-run</code>, and so does <code>metadata-set</code>. They are editors rather than refactorings.</p>
        </AlertDescription>
      </Alert>
      <h2 id="before-you-change-anything" class="group scroll-mt-20">Before you change anything<a href="#before-you-change-anything" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>Ask what depends on it:</p>
      <CodeBlock code={"dogsbay-xml where-used topics/installing-audacity.dita \\\n  --root . --map audacity-guide.ditamap"} lang="bash" />
      <p>With <code>--map</code>, the report includes references that reach the file indirectly through a key, which are the ones a search would miss.</p>
      <h2 id="what-each-refactoring-is-for" class="group scroll-mt-20">What each refactoring is for<a href="#what-each-refactoring-is-for" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <h3 id="moving-and-renaming" class="group scroll-mt-20">Moving and renaming<a href="#moving-and-renaming" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>Use it when</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>rename-file</code></TableCell>
      <TableCell>A file's name or location is wrong.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>rename-key</code></TableCell>
      <TableCell>A key's name is wrong, or you are aligning a naming scheme.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>rename-element-id</code></TableCell>
      <TableCell>An element ID is wrong and conrefs point at it.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>rename-profile-value</code></TableCell>
      <TableCell>A condition value changes, such as <code>mac</code> becoming <code>macos</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>delete-file</code></TableCell>
      <TableCell>A file should go. It reports inbound references first, so you find out before rather than after.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>retarget</code></TableCell>
      <TableCell>Two files should become one: point every reference from one at the other.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="changing-how-content-is-referenced" class="group scroll-mt-20">Changing how content is referenced<a href="#changing-how-content-is-referenced" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>Use it when</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>keyify</code></TableCell>
      <TableCell>A file is referenced by path in many places and should be a key.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>inline-key</code></TableCell>
      <TableCell>A key is not earning its indirection.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>extract-conref</code></TableCell>
      <TableCell>The same content is repeated and should be reused from one place.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>inline-conref</code></TableCell>
      <TableCell>Reused content should become a local copy again.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>create-keydef</code></TableCell>
      <TableCell>You want a text key, such as a product name, defined once.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>merge-keydefs</code></TableCell>
      <TableCell>A map's closure defines the same key more than once and only the first can win.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="restructuring" class="group scroll-mt-20">Restructuring<a href="#restructuring" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>Use it when</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>split-topic</code></TableCell>
      <TableCell>A topic has grown into several. Each top-level section becomes a topic, and the map is updated to include them.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="a-worked-example" class="group scroll-mt-20">A worked example<a href="#a-worked-example" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>This example turns a hardcoded product name into a key. It fixes one of the problems in the sample project.</p>
      <Steps>
      <Step>
      <strong>Define the key</strong> The key name and the text it resolves to are the two arguments; the map that receives the definition is an option.
      <CodeBlock code={"dogsbay-xml create-keydef product-name Audacity \\\n  --map keydefs-product.ditamap --apply"} lang="bash" />
      </Step>
      <Step>
      <strong>Replace the hardcoded text as well</strong> <code>--replace-in</code> rewrites whole-word occurrences of the text under a root into key references, which is the part that would otherwise be a hundred manual edits.
      <CodeBlock code={"dogsbay-xml create-keydef product-name Audacity \\\n  --map keydefs-product.ditamap --replace-in topics"} lang="bash" />
      </Step>
      <Step>
      <strong>Apply it</strong> Run the same command with <code>--apply</code> once the plan looks right.
      </Step>
      <Step>
      <strong>Check the result</strong>
      <CodeBlock code={"dogsbay-xml project-health . --map audacity-guide.ditamap"} lang="bash" />
      </Step>
      </Steps>
      <h2 id="when-a-refactoring-refuses" class="group scroll-mt-20">When a refactoring refuses<a href="#when-a-refactoring-refuses" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>If a refactoring would create a broken reference, it reports the problem instead of making the change. Review the plan to find unsafe changes before they affect the output.</p>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./validation" class="[text-decoration:none] text-foreground">
      <Icon name="check" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Validating a project</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Find what needs fixing, and confirm the result.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/authoring/keys-and-reuse" class="[text-decoration:none] text-foreground">
      <Icon name="key" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Keys and reuse</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">The mechanisms these refactorings move between.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: finding/validation.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";

const headings = [{"depth":1,"slug":"validating-a-project","text":"Validating a project"},{"depth":2,"slug":"one-document","text":"One document"},{"depth":2,"slug":"a-whole-set","text":"A whole set"},{"depth":2,"slug":"business-rules-with-schematron","text":"Business rules with Schematron"},{"depth":2,"slug":"the-whole-picture","text":"The whole picture"},{"depth":2,"slug":"what-static-validation-cannot-catch","text":"What static validation cannot catch"},{"depth":2,"slug":"using-these-as-a-gate","text":"Using these as a gate"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/finding/validation";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Validating a project";
const description = "Check one document or a whole set against its grammar, business rules and references, and use the same checks as a build gate." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "how-to";
const pageAudience = null;
const pageCategory = ["finding"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Validating a project {#validating-a-project}\n\nValidation in DogsBay XML answers three separate questions, and it is worth keeping them apart because they fail for different reasons.\n\n**Is this well-formed and legal?** The document parses and matches its grammar, whether that is a DTD, an XML Schema or RelaxNG.\n\n**Does it follow our rules?** Business rules a grammar cannot express: every topic has a short description, no hardcoded product names, metadata is present. This is Schematron.\n\n**Does it hold together?** References resolve, keys are defined, no topic is orphaned. This is a property of the project, not of any file.\n\n## One document {#one-document}\n\nGrammar validation resolves catalogs, so a DITA topic validates against the grammar its DOCTYPE names. A DITA file with no explicit schema falls back to the bundled DITA grammars, which means it validates the same way in the editor and headless.\n\n```bash\nbin/dogsbay-xml validate topics/installing-audacity.dita\n```\n\nIn the editor, problems appear in the margin as you type and in the error pane.\n\n## A whole set {#a-whole-set}\n\n```bash\nbin/dogsbay-xml validate-project . --map audacity-guide.ditamap\n```\n\nThe scope decides what is checked. With `--map`, it is that map's publication set, which is what a build would include. With `--scope` you can point at a glob or the project root instead.\n\nIt exits with a non-zero status when any file is invalid.\n\n## Business rules with Schematron {#business-rules-with-schematron}\n\nSchematron expresses the rules your house style cares about. The sample project ships one, `house-style.sch`, which requires a short description on every topic and forbids hardcoded product names in prose.\n\n```bash\nbin/dogsbay-xml schematron topics/installing-audacity.dita house-style.sch\nbin/dogsbay-xml schematron-project . house-style.sch\n```\n\nA rule that fails here fails the same way in a pipeline, because it is the same engine.\n\n## The whole picture {#the-whole-picture}\n\n`project-health` runs the reference and key analysis, grammar validation and the conref element-id audit together, and reports open agent proposals:\n\n```bash\nbin/dogsbay-xml project-health . --map audacity-guide.ditamap\n```\n\nA clean result is the publish-ready gate. Adding `--map` enables key analysis and scopes validation to the publication set, which is usually what you want: without it, files no map includes are validated too, and orphans are reported rather than skipped.\n\n## What static validation cannot catch {#what-static-validation-cannot-catch}\n\nA key reference can be well-formed, legal and still fail to resolve at build time, because resolution depends on the map, the conditions and the scopes. The only way to be sure is to run the resolution:\n\n```bash\nbin/dogsbay-xml validate-ot .\n```\n\nThat runs DITA-OT preprocessing per deliverable and reports what it cannot resolve. It needs DITA-OT, and it is slower than everything above, which is why it is a separate command rather than part of `project-health`.\n\n## Using these as a gate {#using-these-as-a-gate}\n\nEach of these exits non-zero when it finds something, so a pipeline can be a short list:\n\n```bash\nbin/dogsbay-xml validate-project . --map audacity-guide.ditamap\nbin/dogsbay-xml schematron-project . house-style.sch\nbin/dogsbay-xml metadata-audit . --map audacity-guide.ditamap\nbin/dogsbay-xml validate-conditions . --map audacity-guide.ditamap\nbin/dogsbay-xml project-health . --map audacity-guide.ditamap\n```\n\nRun the cheap checks first. `validate-ot` belongs at the end, or on a schedule, rather than on every commit.\n\n## Related {#related}\n\n:::cards\n- **[Refactoring](./refactoring){icon=\"wand\"}**\n  Fixing what validation finds, without breaking references.\n\n- **[Metadata](/dogsbay-xml-docs/authoring/metadata){icon=\"tag\"}**\n  Auditing against the metadata policy.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/finding/validation.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="validating-a-project" class="group scroll-mt-20">Validating a project<a href="#validating-a-project" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Validation in DogsBay XML answers three separate questions, and it is worth keeping them apart because they fail for different reasons.</p>
      <p><strong>Is this well-formed and legal?</strong> The document parses and matches its grammar, whether that is a DTD, an XML Schema or RelaxNG.</p>
      <p><strong>Does it follow our rules?</strong> Business rules a grammar cannot express: every topic has a short description, no hardcoded product names, metadata is present. This is Schematron.</p>
      <p><strong>Does it hold together?</strong> References resolve, keys are defined, no topic is orphaned. This is a property of the project, not of any file.</p>
      <h2 id="one-document" class="group scroll-mt-20">One document<a href="#one-document" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Grammar validation resolves catalogs, so a DITA topic validates against the grammar its DOCTYPE names. A DITA file with no explicit schema falls back to the bundled DITA grammars, which means it validates the same way in the editor and headless.</p>
      <CodeBlock code={"bin/dogsbay-xml validate topics/installing-audacity.dita"} lang="bash" />
      <p>In the editor, problems appear in the margin as you type and in the error pane.</p>
      <h2 id="a-whole-set" class="group scroll-mt-20">A whole set<a href="#a-whole-set" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <CodeBlock code={"bin/dogsbay-xml validate-project . --map audacity-guide.ditamap"} lang="bash" />
      <p>The scope decides what is checked. With <code>--map</code>, it is that map's publication set, which is what a build would include. With <code>--scope</code> you can point at a glob or the project root instead.</p>
      <p>It exits with a non-zero status when any file is invalid.</p>
      <h2 id="business-rules-with-schematron" class="group scroll-mt-20">Business rules with Schematron<a href="#business-rules-with-schematron" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Schematron expresses the rules your house style cares about. The sample project ships one, <code>house-style.sch</code>, which requires a short description on every topic and forbids hardcoded product names in prose.</p>
      <CodeBlock code={"bin/dogsbay-xml schematron topics/installing-audacity.dita house-style.sch\nbin/dogsbay-xml schematron-project . house-style.sch"} lang="bash" />
      <p>A rule that fails here fails the same way in a pipeline, because it is the same engine.</p>
      <h2 id="the-whole-picture" class="group scroll-mt-20">The whole picture<a href="#the-whole-picture" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p><code>project-health</code> runs the reference and key analysis, grammar validation and the conref element-id audit together, and reports open agent proposals:</p>
      <CodeBlock code={"bin/dogsbay-xml project-health . --map audacity-guide.ditamap"} lang="bash" />
      <p>A clean result is the publish-ready gate. Adding <code>--map</code> enables key analysis and scopes validation to the publication set, which is usually what you want: without it, files no map includes are validated too, and orphans are reported rather than skipped.</p>
      <h2 id="what-static-validation-cannot-catch" class="group scroll-mt-20">What static validation cannot catch<a href="#what-static-validation-cannot-catch" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A key reference can be well-formed, legal and still fail to resolve at build time, because resolution depends on the map, the conditions and the scopes. The only way to be sure is to run the resolution:</p>
      <CodeBlock code={"bin/dogsbay-xml validate-ot ."} lang="bash" />
      <p>That runs DITA-OT preprocessing per deliverable and reports what it cannot resolve. It needs DITA-OT, and it is slower than everything above, which is why it is a separate command rather than part of <code>project-health</code>.</p>
      <h2 id="using-these-as-a-gate" class="group scroll-mt-20">Using these as a gate<a href="#using-these-as-a-gate" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Each of these exits non-zero when it finds something, so a pipeline can be a short list:</p>
      <CodeBlock code={"bin/dogsbay-xml validate-project . --map audacity-guide.ditamap\nbin/dogsbay-xml schematron-project . house-style.sch\nbin/dogsbay-xml metadata-audit . --map audacity-guide.ditamap\nbin/dogsbay-xml validate-conditions . --map audacity-guide.ditamap\nbin/dogsbay-xml project-health . --map audacity-guide.ditamap"} lang="bash" />
      <p>Run the cheap checks first. <code>validate-ot</code> belongs at the end, or on a schedule, rather than on every commit.</p>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./refactoring" class="[text-decoration:none] text-foreground">
      <Icon name="wand" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Refactoring</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Fixing what validation finds, without breaking references.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/authoring/metadata" class="[text-decoration:none] text-foreground">
      <Icon name="tag" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Metadata</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Auditing against the metadata policy.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

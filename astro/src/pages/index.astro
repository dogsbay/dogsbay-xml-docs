---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: index.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";

const headings = [{"depth":1,"slug":"dogsbay-xml","text":"DogsBay XML"},{"depth":2,"slug":"what-it-is-built-around","text":"What it is built around"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "DogsBay XML";
const description = "An XML and DITA editor with an AI agent that works on your content model and shows you every change before it lands." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "explanation";
const pageAudience = null;
const pageCategory = null;
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# DogsBay XML {#dogsbay-xml}\n\nDogsBay XML is a desktop editor for DITA and other XML. It validates against your grammar as you type, resolves keys and reuse across the whole project, and refactors without breaking references.\n\nIt also has an AI agent. The difference from an agent added to a text editor is that this one calls the editor's own DITA operations, and its edits arrive as proposals that you accept or reject.\n\n:::cards\n- **[Start here](/dogsbay-xml-docs/getting-started/install){icon=\"rocket\"}**\n  Install the editor, open the sample project, and fix a real DITA guide with the agent.\n\n- **[Working with the agent](/dogsbay-xml-docs/agent/overview){icon=\"sparkles\"}**\n  Built-in and hosted agents, what each one is allowed to touch, and how their changes are reviewed.\n\n- **[Authoring DITA](/dogsbay-xml-docs/authoring/topics-and-maps){icon=\"file-text\"}**\n  Topics, maps, keys, reuse and conditional content.\n\n- **[Reference](/dogsbay-xml-docs/reference/cli){icon=\"book\"}**\n  Commands, tools and configuration.\n:::\n\n## What it is built around {#what-it-is-built-around}\n\n**One engine, three interfaces.** The editor's menus, the `dogsbay-xml` command line, and the MCP server that AI assistants connect to are not separate implementations. They run the same commands. A rule that fails validation while you type fails the same way in a pipeline.\n\n**Changes are reviewable before they land.** Refactorings report what they would do and change nothing until you pass `--apply`. Edits an agent makes to a DITA file arrive as tracked changes that you accept or reject, marked with the identity that proposed them.\n\n**The project, not the file.** Keys, conrefs, maps and relationship tables describe how files relate to each other, so the operations that matter work across all of them: find every reference to a topic, rename a key everywhere, or check that a build's conditions use values your subject scheme allows.\n\n> [!NOTE]\n> The integration server that AI assistants and the live-editor commands connect to is turned off by default. Turn it on in **File > Preferences > Server** when you need it. The built-in agent does not require it.\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/index.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="dogsbay-xml" class="group scroll-mt-20">DogsBay XML<a href="#dogsbay-xml" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>DogsBay XML is a desktop editor for DITA and other XML. It validates against your grammar as you type, resolves keys and reuse across the whole project, and refactors without breaking references.</p>
      <p>It also has an AI agent. The difference from an agent added to a text editor is that this one calls the editor's own DITA operations, and its edits arrive as proposals that you accept or reject.</p>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/getting-started/install" class="[text-decoration:none] text-foreground">
      <Icon name="rocket" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Start here</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Install the editor, open the sample project, and fix a real DITA guide with the agent.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/agent/overview" class="[text-decoration:none] text-foreground">
      <Icon name="sparkles" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Working with the agent</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Built-in and hosted agents, what each one is allowed to touch, and how their changes are reviewed.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/authoring/topics-and-maps" class="[text-decoration:none] text-foreground">
      <Icon name="file-text" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Authoring DITA</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Topics, maps, keys, reuse and conditional content.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/reference/cli" class="[text-decoration:none] text-foreground">
      <Icon name="book" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Reference</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Commands, tools and configuration.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
      <h2 id="what-it-is-built-around" class="group scroll-mt-20">What it is built around<a href="#what-it-is-built-around" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p><strong>One engine, three interfaces.</strong> The editor's menus, the <code>dogsbay-xml</code> command line, and the MCP server that AI assistants connect to are not separate implementations. They run the same commands. A rule that fails validation while you type fails the same way in a pipeline.</p>
      <p><strong>Changes are reviewable before they land.</strong> Refactorings report what they would do and change nothing until you pass <code>--apply</code>. Edits an agent makes to a DITA file arrive as tracked changes that you accept or reject, marked with the identity that proposed them.</p>
      <p><strong>The project, not the file.</strong> Keys, conrefs, maps and relationship tables describe how files relate to each other, so the operations that matter work across all of them: find every reference to a topic, rename a key everywhere, or check that a build's conditions use values your subject scheme allows.</p>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The integration server that AI assistants and the live-editor commands connect to is turned off by default. Turn it on in <strong>File &gt; Preferences &gt; Server</strong> when you need it. The built-in agent does not require it.</p>
        </AlertDescription>
      </Alert>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

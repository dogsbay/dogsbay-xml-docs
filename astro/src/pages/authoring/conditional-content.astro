---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: authoring/conditional-content.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Step from "@ui/steps/Step.astro";
import Steps from "@ui/steps/Steps.astro";

const headings = [{"depth":1,"slug":"conditional-content","text":"Conditional content"},{"depth":2,"slug":"marking-content","text":"Marking content"},{"depth":2,"slug":"filtering-a-build","text":"Filtering a build"},{"depth":2,"slug":"branch-filtering","text":"Branch filtering"},{"depth":2,"slug":"the-problem-with-uncontrolled-values","text":"The problem with uncontrolled values"},{"depth":2,"slug":"controlling-values-with-a-subject-scheme","text":"Controlling values with a subject scheme"},{"depth":2,"slug":"renaming-a-value-everywhere","text":"Renaming a value everywhere"},{"depth":2,"slug":"a-useful-order","text":"A useful order"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/authoring/conditional-content";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Conditional content";
const description = "Profile content with conditions, filter it with DITAVAL, and keep the values under control with a subject scheme." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "how-to";
const pageAudience = null;
const pageCategory = ["authoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Conditional content {#conditional-content}\n\nConditional content is one source producing several outputs: a Windows guide and a macOS guide from the same topics, or a beginner version and an expert version. You mark content with profiling attributes, and a DITAVAL file decides what each build includes.\n\nThe mechanism is simple. What goes wrong is that the values drift.\n\n## Marking content {#marking-content}\n\nProfiling attributes such as `platform`, `audience` and `product` go on any element. The editor knows which values are in use in the project, so you are choosing from what exists rather than typing a new one by accident.\n\n## Filtering a build {#filtering-a-build}\n\nA DITAVAL file lists which values to include and exclude. The sample project keeps them in `filters/`:\n\n```\nfilters/platform-macos.ditaval\nfilters/platform-windows.ditaval\nfilters/mac-beginner.ditaval\n```\n\nA DITAVAL affects more than the text that survives. It also conditions the key space, so a key can resolve to different targets in different builds:\n\n```bash\ndogsbay-xml keys audacity-guide.ditamap --ditaval filters/platform-macos.ditaval\n```\n\n## Branch filtering {#branch-filtering}\n\nDITA 1.3 lets a map apply a DITAVAL to one branch, with `ditavalref`, so the same topics appear more than once in one map under different conditions. That is how one map produces a Windows chapter and a macOS chapter.\n\nTo see the variants a map produces:\n\n```bash\ndogsbay-xml list-branches audacity-guide.ditamap\n```\n\nBranch filtering is resolved by DITA-OT at build time. The editor shows you the variants a map declares; it does not itself produce a filtered build.\n\n## The problem with uncontrolled values {#the-problem-with-uncontrolled-values}\n\nNothing in DITA stops you writing `platform=\"macos\"` in one topic and `platform=\"mac\"` in another. Both are valid XML and both validate against the grammar.\n\nThe build then silently drops the topic that used the value your DITAVAL does not mention. Nothing errors. The output is simply missing a step, and nobody notices until a reader does.\n\n## Controlling values with a subject scheme {#controlling-values-with-a-subject-scheme}\n\nA subject scheme map declares which values an attribute may take. It turns a typo from an invisible content loss into something you can find.\n\nTo see what a scheme allows:\n\n```bash\ndogsbay-xml list-subjects keydefs-glossary.ditamap\n```\n\nTo find content that breaks it:\n\n```bash\ndogsbay-xml validate-conditions . --map audacity-guide.ditamap\n```\n\nThat scans the map's publication set and reports every profiling value the scheme does not sanction. It exits with a non-zero status when it finds any, so it works as a pipeline gate.\n\nUse `--scope` to narrow it to one map, a glob, or the whole project root.\n\n## Renaming a value everywhere {#renaming-a-value-everywhere}\n\nWhen a value does need to change, change it as a refactoring rather than by search and replace, so every use moves together:\n\n```bash\ndogsbay-xml rename-profile-value platform mac macos --root .\ndogsbay-xml rename-profile-value platform mac macos --root . --apply\n```\n\nThe first prints the plan; the second applies it.\n\n## A useful order {#a-useful-order}\n\n:::steps\n1. **Declare the vocabulary** Write the subject scheme first, so the values exist before content uses them.\n2. **Check what is already there** Run `validate-conditions` over the existing content and fix what it finds with `rename-profile-value`.\n3. **Add the check to your pipeline** It exits non-zero, so a build fails on a new typo rather than shipping without the content.\n:::\n\n## Related {#related}\n\n:::cards\n- **[Keys and reuse](./keys-and-reuse){icon=\"key\"}**\n  Why the same key resolves differently under different conditions.\n\n- **[Publishing](/dogsbay-xml-docs/publishing/publishing){icon=\"package\"}**\n  Deliverables, each with its own map and DITAVAL.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/authoring/conditional-content.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="conditional-content" class="group scroll-mt-20">Conditional content<a href="#conditional-content" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Conditional content is one source producing several outputs: a Windows guide and a macOS guide from the same topics, or a beginner version and an expert version. You mark content with profiling attributes, and a DITAVAL file decides what each build includes.</p>
      <p>The mechanism is simple. What goes wrong is that the values drift.</p>
      <h2 id="marking-content" class="group scroll-mt-20">Marking content<a href="#marking-content" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Profiling attributes such as <code>platform</code>, <code>audience</code> and <code>product</code> go on any element. The editor knows which values are in use in the project, so you are choosing from what exists rather than typing a new one by accident.</p>
      <h2 id="filtering-a-build" class="group scroll-mt-20">Filtering a build<a href="#filtering-a-build" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A DITAVAL file lists which values to include and exclude. The sample project keeps them in <code>filters/</code>:</p>
      <CodeBlock code={"filters/platform-macos.ditaval\nfilters/platform-windows.ditaval\nfilters/mac-beginner.ditaval"} lang="plaintext" />
      <p>A DITAVAL affects more than the text that survives. It also conditions the key space, so a key can resolve to different targets in different builds:</p>
      <CodeBlock code={"dogsbay-xml keys audacity-guide.ditamap --ditaval filters/platform-macos.ditaval"} lang="bash" />
      <h2 id="branch-filtering" class="group scroll-mt-20">Branch filtering<a href="#branch-filtering" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>DITA 1.3 lets a map apply a DITAVAL to one branch, with <code>ditavalref</code>, so the same topics appear more than once in one map under different conditions. That is how one map produces a Windows chapter and a macOS chapter.</p>
      <p>To see the variants a map produces:</p>
      <CodeBlock code={"dogsbay-xml list-branches audacity-guide.ditamap"} lang="bash" />
      <p>Branch filtering is resolved by DITA-OT at build time. The editor shows you the variants a map declares; it does not itself produce a filtered build.</p>
      <h2 id="the-problem-with-uncontrolled-values" class="group scroll-mt-20">The problem with uncontrolled values<a href="#the-problem-with-uncontrolled-values" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Nothing in DITA stops you writing <code>platform="macos"</code> in one topic and <code>platform="mac"</code> in another. Both are valid XML and both validate against the grammar.</p>
      <p>The build then silently drops the topic that used the value your DITAVAL does not mention. Nothing errors. The output is simply missing a step, and nobody notices until a reader does.</p>
      <h2 id="controlling-values-with-a-subject-scheme" class="group scroll-mt-20">Controlling values with a subject scheme<a href="#controlling-values-with-a-subject-scheme" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A subject scheme map declares which values an attribute may take. It turns a typo from an invisible content loss into something you can find.</p>
      <p>To see what a scheme allows:</p>
      <CodeBlock code={"dogsbay-xml list-subjects keydefs-glossary.ditamap"} lang="bash" />
      <p>To find content that breaks it:</p>
      <CodeBlock code={"dogsbay-xml validate-conditions . --map audacity-guide.ditamap"} lang="bash" />
      <p>That scans the map's publication set and reports every profiling value the scheme does not sanction. It exits with a non-zero status when it finds any, so it works as a pipeline gate.</p>
      <p>Use <code>--scope</code> to narrow it to one map, a glob, or the whole project root.</p>
      <h2 id="renaming-a-value-everywhere" class="group scroll-mt-20">Renaming a value everywhere<a href="#renaming-a-value-everywhere" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>When a value does need to change, change it as a refactoring rather than by search and replace, so every use moves together:</p>
      <CodeBlock code={"dogsbay-xml rename-profile-value platform mac macos --root .\ndogsbay-xml rename-profile-value platform mac macos --root . --apply"} lang="bash" />
      <p>The first prints the plan; the second applies it.</p>
      <h2 id="a-useful-order" class="group scroll-mt-20">A useful order<a href="#a-useful-order" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Steps>
      <Step>
      <strong>Declare the vocabulary</strong> Write the subject scheme first, so the values exist before content uses them.
      </Step>
      <Step>
      <strong>Check what is already there</strong> Run <code>validate-conditions</code> over the existing content and fix what it finds with <code>rename-profile-value</code>.
      </Step>
      <Step>
      <strong>Add the check to your pipeline</strong> It exits non-zero, so a build fails on a new typo rather than shipping without the content.
      </Step>
      </Steps>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./keys-and-reuse" class="[text-decoration:none] text-foreground">
      <Icon name="key" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Keys and reuse</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Why the same key resolves differently under different conditions.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/publishing/publishing" class="[text-decoration:none] text-foreground">
      <Icon name="package" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Publishing</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Deliverables, each with its own map and DITAVAL.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

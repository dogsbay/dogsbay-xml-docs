---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: authoring/metadata.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Step from "@ui/steps/Step.astro";
import Steps from "@ui/steps/Steps.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"metadata","text":"Metadata"},{"depth":2,"slug":"where-the-policy-lives","text":"Where the policy lives"},{"depth":2,"slug":"auditing","text":"Auditing"},{"depth":2,"slug":"filling-in-what-is-missing","text":"Filling in what is missing"},{"depth":2,"slug":"enforcing-the-policy-outside-the-editor","text":"Enforcing the policy outside the editor"},{"depth":2,"slug":"a-useful-order","text":"A useful order"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/authoring/metadata";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Metadata";
const description = "Define required topic metadata, audit the project, and fill in missing fields." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "how-to";
const pageAudience = null;
const pageCategory = ["authoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Metadata {#metadata}\n\nMetadata in a documentation set is useful only when it is consistent. A policy defines the fields that topics must contain. The editor audits the content against the policy and fills in missing fields without changing the rest of the prolog.\n\n## Where the policy lives {#where-the-policy-lives}\n\nThe required-metadata policy is part of the project configuration, in `.dogsbay/config.xml`. Because it is a file in the project, it is shared with everyone who clones it and changes with the content. For the rule attributes and field names, see [Project configuration](/dogsbay-xml-docs/reference/project-config#metadata-policy).\n\nYou can point at a different policy for one run with `--policy`.\n\n## Auditing {#auditing}\n\n```bash\ndogsbay-xml metadata-audit . --map audacity-guide.ditamap\n```\n\nThe command reports where required fields are missing or contain a value that the policy does not allow. It exits with a nonzero status on any error-level violation, so it works as a pipeline gate.\n\nUse `--scope` to audit something other than a map's publication set, such as a glob or the project root.\n\n## Filling in what is missing {#filling-in-what-is-missing}\n\n`metadata-set` applies changes in bulk and preserves the fields it is not asked to touch.\n\n```bash\ndogsbay-xml metadata-set . --map audacity-guide.ditamap \\\n  --fill audience=user --dry-run\n```\n\nThe four operations differ in a way worth getting right:\n\n| Option | Effect |\n| --- | --- |\n| `--fill` | Set the field only where it is absent. Existing values are left alone. |\n| `--set` | Overwrite the field everywhere in scope. |\n| `--append` | Add a value to a list field, keeping what is there. |\n| `--remove` | Remove a field, or one value from a list field. |\n\nRepeat an option to change several fields in one pass.\n\n> [!IMPORTANT]\n> `metadata-set` writes unless you pass `--dry-run`. That is the opposite of the refactoring commands, which do nothing until you pass `--apply`. Check with `--dry-run` first on a scope you have not touched before.\n\n## Enforcing the policy outside the editor {#enforcing-the-policy-outside-the-editor}\n\nThe same policy can be compiled to ISO Schematron, so a build system that has no DogsBay XML can still check it:\n\n```bash\ndogsbay-xml metadata-export-schematron . --output metadata-policy.sch\n```\n\nRun the result anywhere Schematron runs, including here:\n\n```bash\ndogsbay-xml schematron-project . metadata-policy.sch\n```\n\n## A useful order {#a-useful-order}\n\n:::steps\n1. **Audit before you change anything** `metadata-audit` tells you the size of the problem.\n2. **Fill the gaps** Use `metadata-set --fill` for fields with a standard default. Check with `--dry-run` first.\n3. **Fix the rest by hand** What is left usually needs a human, which is the point of separating fill from set.\n4. **Gate it** Add `metadata-audit` to the pipeline so the gap does not reopen.\n:::\n\n## Related {#related}\n\n:::cards\n- **[Validating a project](/dogsbay-xml-docs/finding/validation){icon=\"check\"}**\n  Grammar, Schematron, and project-wide checks.\n\n- **[Conditional content](./conditional-content){icon=\"filter\"}**\n  Controlled values for profiling attributes.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/authoring/metadata.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  pagefindBaseUrl={"/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="metadata" class="group scroll-mt-20">Metadata<a href="#metadata" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h1>
      <p>Metadata in a documentation set is useful only when it is consistent. A policy defines the fields that topics must contain. The editor audits the content against the policy and fills in missing fields without changing the rest of the prolog.</p>
      <h2 id="where-the-policy-lives" class="group scroll-mt-20">Where the policy lives<a href="#where-the-policy-lives" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>The required-metadata policy is part of the project configuration, in <code>.dogsbay/config.xml</code>. Because it is a file in the project, it is shared with everyone who clones it and changes with the content. For the rule attributes and field names, see <a href="/dogsbay-xml-docs/reference/project-config#metadata-policy">Project configuration</a>.</p>
      <p>You can point at a different policy for one run with <code>--policy</code>.</p>
      <h2 id="auditing" class="group scroll-mt-20">Auditing<a href="#auditing" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <CodeBlock code={"dogsbay-xml metadata-audit . --map audacity-guide.ditamap"} lang="bash" />
      <p>The command reports where required fields are missing or contain a value that the policy does not allow. It exits with a nonzero status on any error-level violation, so it works as a pipeline gate.</p>
      <p>Use <code>--scope</code> to audit something other than a map's publication set, such as a glob or the project root.</p>
      <h2 id="filling-in-what-is-missing" class="group scroll-mt-20">Filling in what is missing<a href="#filling-in-what-is-missing" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p><code>metadata-set</code> applies changes in bulk and preserves the fields it is not asked to touch.</p>
      <CodeBlock code={"dogsbay-xml metadata-set . --map audacity-guide.ditamap \\\n  --fill audience=user --dry-run"} lang="bash" />
      <p>The four operations differ in a way worth getting right:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Option</TableHead>
      <TableHead>Effect</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>--fill</code></TableCell>
      <TableCell>Set the field only where it is absent. Existing values are left alone.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>--set</code></TableCell>
      <TableCell>Overwrite the field everywhere in scope.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>--append</code></TableCell>
      <TableCell>Add a value to a list field, keeping what is there.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>--remove</code></TableCell>
      <TableCell>Remove a field, or one value from a list field.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Repeat an option to change several fields in one pass.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p><code>metadata-set</code> writes unless you pass <code>--dry-run</code>. That is the opposite of the refactoring commands, which do nothing until you pass <code>--apply</code>. Check with <code>--dry-run</code> first on a scope you have not touched before.</p>
        </AlertDescription>
      </Alert>
      <h2 id="enforcing-the-policy-outside-the-editor" class="group scroll-mt-20">Enforcing the policy outside the editor<a href="#enforcing-the-policy-outside-the-editor" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>The same policy can be compiled to ISO Schematron, so a build system that has no DogsBay XML can still check it:</p>
      <CodeBlock code={"dogsbay-xml metadata-export-schematron . --output metadata-policy.sch"} lang="bash" />
      <p>Run the result anywhere Schematron runs, including here:</p>
      <CodeBlock code={"dogsbay-xml schematron-project . metadata-policy.sch"} lang="bash" />
      <h2 id="a-useful-order" class="group scroll-mt-20">A useful order<a href="#a-useful-order" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Steps>
      <Step>
      <strong>Audit before you change anything</strong> <code>metadata-audit</code> tells you the size of the problem.
      </Step>
      <Step>
      <strong>Fill the gaps</strong> Use <code>metadata-set --fill</code> for fields with a standard default. Check with <code>--dry-run</code> first.
      </Step>
      <Step>
      <strong>Fix the rest by hand</strong> What is left usually needs a human, which is the point of separating fill from set.
      </Step>
      <Step>
      <strong>Gate it</strong> Add <code>metadata-audit</code> to the pipeline so the gap does not reopen.
      </Step>
      </Steps>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/finding/validation" class="[text-decoration:none] text-foreground">
      <Icon name="check" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Validating a project</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Grammar, Schematron, and project-wide checks.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./conditional-content" class="[text-decoration:none] text-foreground">
      <Icon name="filter" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Conditional content</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Controlled values for profiling attributes.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: authoring/keys-and-reuse.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";

const headings = [{"depth":1,"slug":"keys-and-reuse","text":"Keys and reuse"},{"depth":2,"slug":"seeing-the-key-space","text":"Seeing the key space"},{"depth":2,"slug":"converting-direct-references-to-keys","text":"Converting direct references to keys"},{"depth":2,"slug":"moving-shared-content-into-a-reuse-topic","text":"Moving shared content into a reuse topic"},{"depth":2,"slug":"checking-that-reuse-still-resolves","text":"Checking that reuse still resolves"},{"depth":2,"slug":"tidying-key-definitions","text":"Tidying key definitions"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/authoring/keys-and-reuse";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Keys and reuse";
const description = "Resolve keys across a map, convert direct references to keys, and move shared content into reuse topics." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "how-to";
const pageAudience = null;
const pageCategory = ["authoring"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Keys and reuse {#keys-and-reuse}\n\nDITA has two mechanisms for not repeating yourself: keys, which give a name to a target so the reference does not carry a path, and conrefs, which pull content from one topic into another. Both create relationships the editor can show you and change safely.\n\n## Seeing the key space {#seeing-the-key-space}\n\nA map's key space is every key its topics can use, including keys defined in the maps it includes.\n\n```bash\ndogsbay-xml keys audacity-guide.ditamap\n```\n\nTo ask what one key resolves to:\n\n```bash\ndogsbay-xml keys audacity-guide.ditamap --resolve product-name\n```\n\nTwo things change what a key resolves to, and both are options here.\n\n**Conditions.** A DITAVAL can exclude a key definition, so the same key resolves differently in different builds:\n\n```bash\ndogsbay-xml keys audacity-guide.ditamap --ditaval filters/platform-macos.ditaval\n```\n\n**Key scopes.** DITA 1.3 lets a map declare a scope, so a key can mean different things in different branches of the same map:\n\n```bash\ndogsbay-xml keys audacity-guide.ditamap --resolve intro --scope podcaster\n```\n\n> [!TIP]\n> A key that resolves in the editor but not in a build is usually a conditions or scope difference, not a broken key. Resolve it with the same DITAVAL the build uses before looking anywhere else.\n\n## Converting direct references to keys {#converting-direct-references-to-keys}\n\nA file referenced by path in twenty topics is twenty edits when it moves. The `keyify` command converts all of them to key references and adds the key definition to a map you choose.\n\n```bash\ndogsbay-xml keyify topics/installing-audacity.dita installing \\\n  --root . --map keydefs-product.ditamap\n```\n\nThat prints the plan. Add `--apply` to make the change.\n\nThe reverse, when a key is not earning its indirection:\n\n```bash\ndogsbay-xml inline-key installing --root . --map audacity-guide.ditamap --apply\n```\n\nThe map is needed because it is what resolves the key to the path that replaces it.\n\n## Moving shared content into a reuse topic {#moving-shared-content-into-a-reuse-topic}\n\nWhen the same note, step or paragraph appears in several topics, move it once into a reuse topic and conref it everywhere else.\n\n```bash\ndogsbay-xml extract-conref topics/installing-audacity.dita note-backup \\\n  --to shared/common-notes.dita\n```\n\nThe element is moved into the reuse topic, which is created if it does not exist, and a conref stub is left in its place. Add `--apply` to run it.\n\nTo undo that relationship, replacing conrefs with a copy of the content:\n\n```bash\ndogsbay-xml inline-conref shared/common-notes.dita note-backup --root . --apply\n```\n\nUse `--file` to inline only the instances in one topic rather than all of them.\n\n## Checking that reuse still resolves {#checking-that-reuse-still-resolves}\n\nTwo audits answer different questions.\n\n```bash\ndogsbay-xml check-links .\ndogsbay-xml conref-audit .\n```\n\n`check-links` reports references whose target file is missing, and keys used but never defined. `conref-audit` is narrower and catches the failure that is easy to create by accident: the target file exists, but the element id the conref names is no longer in it.\n\nAn element id disappears when someone renames it or deletes the element it was on, which is why renaming ids is a command rather than an edit:\n\n```bash\ndogsbay-xml rename-element-id shared/common-notes.dita note-backup backup-note --apply\n```\n\n## Tidying key definitions {#tidying-key-definitions}\n\nWhen maps are merged or copied, the same key often ends up defined more than once. Only the first definition wins, so the rest are noise:\n\n```bash\ndogsbay-xml merge-keydefs audacity-guide.ditamap --apply\n```\n\n## Related {#related}\n\n:::cards\n- **[Topics and maps](./topics-and-maps){icon=\"file-text\"}**\n  Assembling topics, and renaming without breaking references.\n\n- **[Conditional content](./conditional-content){icon=\"filter\"}**\n  Conditions, branch filtering and controlled values.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/authoring/keys-and-reuse.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="keys-and-reuse" class="group scroll-mt-20">Keys and reuse<a href="#keys-and-reuse" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>DITA has two mechanisms for not repeating yourself: keys, which give a name to a target so the reference does not carry a path, and conrefs, which pull content from one topic into another. Both create relationships the editor can show you and change safely.</p>
      <h2 id="seeing-the-key-space" class="group scroll-mt-20">Seeing the key space<a href="#seeing-the-key-space" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A map's key space is every key its topics can use, including keys defined in the maps it includes.</p>
      <CodeBlock code={"dogsbay-xml keys audacity-guide.ditamap"} lang="bash" />
      <p>To ask what one key resolves to:</p>
      <CodeBlock code={"dogsbay-xml keys audacity-guide.ditamap --resolve product-name"} lang="bash" />
      <p>Two things change what a key resolves to, and both are options here.</p>
      <p><strong>Conditions.</strong> A DITAVAL can exclude a key definition, so the same key resolves differently in different builds:</p>
      <CodeBlock code={"dogsbay-xml keys audacity-guide.ditamap --ditaval filters/platform-macos.ditaval"} lang="bash" />
      <p><strong>Key scopes.</strong> DITA 1.3 lets a map declare a scope, so a key can mean different things in different branches of the same map:</p>
      <CodeBlock code={"dogsbay-xml keys audacity-guide.ditamap --resolve intro --scope podcaster"} lang="bash" />
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p>A key that resolves in the editor but not in a build is usually a conditions or scope difference, not a broken key. Resolve it with the same DITAVAL the build uses before looking anywhere else.</p>
        </AlertDescription>
      </Alert>
      <h2 id="converting-direct-references-to-keys" class="group scroll-mt-20">Converting direct references to keys<a href="#converting-direct-references-to-keys" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A file referenced by path in twenty topics is twenty edits when it moves. The <code>keyify</code> command converts all of them to key references and adds the key definition to a map you choose.</p>
      <CodeBlock code={"dogsbay-xml keyify topics/installing-audacity.dita installing \\\n  --root . --map keydefs-product.ditamap"} lang="bash" />
      <p>That prints the plan. Add <code>--apply</code> to make the change.</p>
      <p>The reverse, when a key is not earning its indirection:</p>
      <CodeBlock code={"dogsbay-xml inline-key installing --root . --map audacity-guide.ditamap --apply"} lang="bash" />
      <p>The map is needed because it is what resolves the key to the path that replaces it.</p>
      <h2 id="moving-shared-content-into-a-reuse-topic" class="group scroll-mt-20">Moving shared content into a reuse topic<a href="#moving-shared-content-into-a-reuse-topic" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>When the same note, step or paragraph appears in several topics, move it once into a reuse topic and conref it everywhere else.</p>
      <CodeBlock code={"dogsbay-xml extract-conref topics/installing-audacity.dita note-backup \\\n  --to shared/common-notes.dita"} lang="bash" />
      <p>The element is moved into the reuse topic, which is created if it does not exist, and a conref stub is left in its place. Add <code>--apply</code> to run it.</p>
      <p>To undo that relationship, replacing conrefs with a copy of the content:</p>
      <CodeBlock code={"dogsbay-xml inline-conref shared/common-notes.dita note-backup --root . --apply"} lang="bash" />
      <p>Use <code>--file</code> to inline only the instances in one topic rather than all of them.</p>
      <h2 id="checking-that-reuse-still-resolves" class="group scroll-mt-20">Checking that reuse still resolves<a href="#checking-that-reuse-still-resolves" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Two audits answer different questions.</p>
      <CodeBlock code={"dogsbay-xml check-links .\ndogsbay-xml conref-audit ."} lang="bash" />
      <p><code>check-links</code> reports references whose target file is missing, and keys used but never defined. <code>conref-audit</code> is narrower and catches the failure that is easy to create by accident: the target file exists, but the element id the conref names is no longer in it.</p>
      <p>An element id disappears when someone renames it or deletes the element it was on, which is why renaming ids is a command rather than an edit:</p>
      <CodeBlock code={"dogsbay-xml rename-element-id shared/common-notes.dita note-backup backup-note --apply"} lang="bash" />
      <h2 id="tidying-key-definitions" class="group scroll-mt-20">Tidying key definitions<a href="#tidying-key-definitions" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>When maps are merged or copied, the same key often ends up defined more than once. Only the first definition wins, so the rest are noise:</p>
      <CodeBlock code={"dogsbay-xml merge-keydefs audacity-guide.ditamap --apply"} lang="bash" />
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./topics-and-maps" class="[text-decoration:none] text-foreground">
      <Icon name="file-text" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Topics and maps</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Assembling topics, and renaming without breaking references.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./conditional-content" class="[text-decoration:none] text-foreground">
      <Icon name="filter" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Conditional content</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Conditions, branch filtering and controlled values.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: publishing/publishing.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"previewing-and-publishing","text":"Previewing and publishing"},{"depth":2,"slug":"previewing-while-you-write","text":"Previewing while you write"},{"depth":2,"slug":"deliverables","text":"Deliverables"},{"depth":2,"slug":"building","text":"Building"},{"depth":2,"slug":"checking-before-you-publish","text":"Checking before you publish"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/publishing/publishing";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Previewing and publishing";
const description = "See a styled preview while you write, then build deliverables with DITA-OT." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "how-to";
const pageAudience = null;
const pageCategory = ["publishing"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Previewing and publishing {#previewing-and-publishing}\n\nA preview is immediate and approximate, so you can check your work as you write. A build is the final output that DITA-OT creates from a deliverable's map, conditions, and parameters.\n\n## Previewing while you write {#previewing-while-you-write}\n\nSelect **View > Preview in Tab**, or **View > Preview in Split** to keep the source beside it. The preview refreshes as you edit.\n\nThe preview is not a plain rendering of the file you have open. It resolves conrefs, so reused content appears where it is pulled in, and it resolves key references when it knows which map provides the context.\n\nFrom the command line:\n\n```bash\ndogsbay-xml preview topics/installing-audacity.dita \\\n  --map audacity-guide.ditamap --output preview.html\n```\n\nTwo options change what you see:\n\n| Option | Effect |\n| --- | --- |\n| `--ditaval` | Apply a filter, so you preview one audience or platform. |\n| `--show-changes` | Render open agent proposals as insertions, deletions, and comments instead of the document as it would read with everything accepted. |\n\nWithout `--output` the HTML goes to standard output.\n\n## Deliverables {#deliverables}\n\nA deliverable defines one output: a map, a transform type, a DITAVAL, parameters, and an output directory. A project usually has several, such as an HTML guide for each platform and a PDF for print.\n\nDeliverables come from a DITA project file, which is a standard rather than something this editor invented, so the same definitions work with DITA-OT directly.\n\nWhen a project has no project file, DogsBay XML uses the default root map. You can therefore build a documentation set that is not yet formalized.\n\n## Building {#building}\n\n```bash\ndogsbay-xml build .\ndogsbay-xml build . html-guide\n```\n\nThe first builds every deliverable; the second builds one by name. The command exits with a nonzero status when any build reports errors, so it works as a pipeline step.\n\nTwo options are worth knowing:\n\n| Option | Effect |\n| --- | --- |\n| `--dita-ot` | Use a different DITA-OT than the one the project configures. |\n| `--output` | Put the output somewhere else, each deliverable under its own subdirectory. |\n\nBuilding requires DITA-OT. The sample project is configured with one already.\n\n## Checking before you publish {#checking-before-you-publish}\n\nA build that succeeds can still be missing content, because a key that fails to resolve does not always stop the build.\n\n```bash\ndogsbay-xml validate-deliverables .\ndogsbay-xml validate-ot .\n```\n\n`validate-deliverables` validates each deliverable with its own map and conditions. `validate-ot` goes further and runs DITA-OT preprocessing, which is the only way to catch a key or conref that resolves in one build and not in another.\n\nBefore either, the cheaper check:\n\n```bash\ndogsbay-xml project-health . --map audacity-guide.ditamap\n```\n\n## Related {#related}\n\n:::cards\n- **[Conditional content](/dogsbay-xml-docs/authoring/conditional-content){icon=\"filter\"}**\n  What each deliverable's DITAVAL includes and excludes.\n\n- **[Validating a project](/dogsbay-xml-docs/finding/validation){icon=\"check\"}**\n  The checks worth running before a build.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/publishing/publishing.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="previewing-and-publishing" class="group scroll-mt-20">Previewing and publishing<a href="#previewing-and-publishing" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>A preview is immediate and approximate, so you can check your work as you write. A build is the final output that DITA-OT creates from a deliverable's map, conditions, and parameters.</p>
      <h2 id="previewing-while-you-write" class="group scroll-mt-20">Previewing while you write<a href="#previewing-while-you-write" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Select <strong>View &gt; Preview in Tab</strong>, or <strong>View &gt; Preview in Split</strong> to keep the source beside it. The preview refreshes as you edit.</p>
      <p>The preview is not a plain rendering of the file you have open. It resolves conrefs, so reused content appears where it is pulled in, and it resolves key references when it knows which map provides the context.</p>
      <p>From the command line:</p>
      <CodeBlock code={"dogsbay-xml preview topics/installing-audacity.dita \\\n  --map audacity-guide.ditamap --output preview.html"} lang="bash" />
      <p>Two options change what you see:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Option</TableHead>
      <TableHead>Effect</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>--ditaval</code></TableCell>
      <TableCell>Apply a filter, so you preview one audience or platform.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>--show-changes</code></TableCell>
      <TableCell>Render open agent proposals as insertions, deletions, and comments instead of the document as it would read with everything accepted.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Without <code>--output</code> the HTML goes to standard output.</p>
      <h2 id="deliverables" class="group scroll-mt-20">Deliverables<a href="#deliverables" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A deliverable defines one output: a map, a transform type, a DITAVAL, parameters, and an output directory. A project usually has several, such as an HTML guide for each platform and a PDF for print.</p>
      <p>Deliverables come from a DITA project file, which is a standard rather than something this editor invented, so the same definitions work with DITA-OT directly.</p>
      <p>When a project has no project file, DogsBay XML uses the default root map. You can therefore build a documentation set that is not yet formalized.</p>
      <h2 id="building" class="group scroll-mt-20">Building<a href="#building" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <CodeBlock code={"dogsbay-xml build .\ndogsbay-xml build . html-guide"} lang="bash" />
      <p>The first builds every deliverable; the second builds one by name. The command exits with a nonzero status when any build reports errors, so it works as a pipeline step.</p>
      <p>Two options are worth knowing:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Option</TableHead>
      <TableHead>Effect</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>--dita-ot</code></TableCell>
      <TableCell>Use a different DITA-OT than the one the project configures.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>--output</code></TableCell>
      <TableCell>Put the output somewhere else, each deliverable under its own subdirectory.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Building requires DITA-OT. The sample project is configured with one already.</p>
      <h2 id="checking-before-you-publish" class="group scroll-mt-20">Checking before you publish<a href="#checking-before-you-publish" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A build that succeeds can still be missing content, because a key that fails to resolve does not always stop the build.</p>
      <CodeBlock code={"dogsbay-xml validate-deliverables .\ndogsbay-xml validate-ot ."} lang="bash" />
      <p><code>validate-deliverables</code> validates each deliverable with its own map and conditions. <code>validate-ot</code> goes further and runs DITA-OT preprocessing, which is the only way to catch a key or conref that resolves in one build and not in another.</p>
      <p>Before either, the cheaper check:</p>
      <CodeBlock code={"dogsbay-xml project-health . --map audacity-guide.ditamap"} lang="bash" />
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/authoring/conditional-content" class="[text-decoration:none] text-foreground">
      <Icon name="filter" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Conditional content</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">What each deliverable's DITAVAL includes and excludes.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/finding/validation" class="[text-decoration:none] text-foreground">
      <Icon name="check" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Validating a project</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">The checks worth running before a build.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

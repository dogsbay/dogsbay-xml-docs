---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: getting-started/what-it-is.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";

const headings = [{"depth":1,"slug":"what-dogsbay-xml-is","text":"What DogsBay XML is"},{"depth":2,"slug":"who-it-is-for","text":"Who it is for"},{"depth":2,"slug":"three-ideas","text":"Three ideas"},{"depth":3,"slug":"one-engine-three-interfaces","text":"One engine, three interfaces"},{"depth":3,"slug":"nothing-changes-until-you-say-so","text":"Nothing changes until you say so"},{"depth":3,"slug":"the-project-is-the-unit","text":"The project is the unit"},{"depth":2,"slug":"what-it-is-not","text":"What it is not"},{"depth":2,"slug":"where-to-start","text":"Where to start"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/getting-started/what-it-is";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "What DogsBay XML is";
const description = "What the editor does, who it is for, and the three ideas that shape it." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "explanation";
const pageAudience = null;
const pageCategory = ["getting-started"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# What DogsBay XML is {#what-dogsbay-xml-is}\n\nDogsBay XML is a desktop editor for DITA and other XML, aimed at people who maintain a documentation set rather than a single file.\n\nIt validates against your grammar as you type, resolves keys and reuse across the project, refactors without breaking references, publishes through DITA-OT, and runs an AI agent that works on the same model the editor does.\n\n## Who it is for {#who-it-is-for}\n\nA technical writer working in DITA, in a set large enough that the relationships between files matter: keys that resolve differently in different maps, topics reused by conref in three places, conditions that decide what a build contains.\n\nIf your set is a handful of files, most of this is unnecessary. The features earn their place when a rename can break something you cannot see from the file you are editing.\n\n## Three ideas {#three-ideas}\n\n### One engine, three interfaces {#one-engine-three-interfaces}\n\nThe editor's menus, the `dogsbay-xml` command line and the MCP server that AI assistants connect to are not separate implementations of the same ideas. They run the same commands.\n\nThe consequence is worth stating plainly: a check that passes in the editor passes the same way in a pipeline, because it is the same check. There is no second implementation to drift.\n\n### Nothing changes until you say so {#nothing-changes-until-you-say-so}\n\nRefactorings report what they would do and change nothing. You pass `--apply` when the plan is right. In the editor the same plan appears as a table you review before it runs.\n\nThe same principle governs the agent. Its edits to DITA files arrive as tracked changes attributed to it, and you accept or reject them.\n\n### The project is the unit {#the-project-is-the-unit}\n\nKeys, conrefs, maps and relationship tables describe how files relate. So the operations that matter work across all of them at once: every reference to a topic, every use of a key, every condition value that no subject scheme allows, every topic no map includes.\n\nThat is also why the agent is useful here. It is not editing text in a file; it is calling operations that already understand the project.\n\n## What it is not {#what-it-is-not}\n\nIt is not a general-purpose text editor, and it is not a CMS. It edits files on disk, in a folder you control, under whatever version control you already use.\n\n## Where to start {#where-to-start}\n\n:::cards\n- **[Installing](./install){icon=\"download\"}**\n  Packages for Linux, macOS and Windows, or build from source.\n\n- **[Tutorial: fixing a DITA guide](./tutorial){icon=\"rocket\"}**\n  Thirty minutes with a real, deliberately broken project.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/getting-started/what-it-is.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="what-dogsbay-xml-is" class="group scroll-mt-20">What DogsBay XML is<a href="#what-dogsbay-xml-is" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>DogsBay XML is a desktop editor for DITA and other XML, aimed at people who maintain a documentation set rather than a single file.</p>
      <p>It validates against your grammar as you type, resolves keys and reuse across the project, refactors without breaking references, publishes through DITA-OT, and runs an AI agent that works on the same model the editor does.</p>
      <h2 id="who-it-is-for" class="group scroll-mt-20">Who it is for<a href="#who-it-is-for" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>A technical writer working in DITA, in a set large enough that the relationships between files matter: keys that resolve differently in different maps, topics reused by conref in three places, conditions that decide what a build contains.</p>
      <p>If your set is a handful of files, most of this is unnecessary. The features earn their place when a rename can break something you cannot see from the file you are editing.</p>
      <h2 id="three-ideas" class="group scroll-mt-20">Three ideas<a href="#three-ideas" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <h3 id="one-engine-three-interfaces" class="group scroll-mt-20">One engine, three interfaces<a href="#one-engine-three-interfaces" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The editor's menus, the <code>dogsbay-xml</code> command line and the MCP server that AI assistants connect to are not separate implementations of the same ideas. They run the same commands.</p>
      <p>The consequence is worth stating plainly: a check that passes in the editor passes the same way in a pipeline, because it is the same check. There is no second implementation to drift.</p>
      <h3 id="nothing-changes-until-you-say-so" class="group scroll-mt-20">Nothing changes until you say so<a href="#nothing-changes-until-you-say-so" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Refactorings report what they would do and change nothing. You pass <code>--apply</code> when the plan is right. In the editor the same plan appears as a table you review before it runs.</p>
      <p>The same principle governs the agent. Its edits to DITA files arrive as tracked changes attributed to it, and you accept or reject them.</p>
      <h3 id="the-project-is-the-unit" class="group scroll-mt-20">The project is the unit<a href="#the-project-is-the-unit" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Keys, conrefs, maps and relationship tables describe how files relate. So the operations that matter work across all of them at once: every reference to a topic, every use of a key, every condition value that no subject scheme allows, every topic no map includes.</p>
      <p>That is also why the agent is useful here. It is not editing text in a file; it is calling operations that already understand the project.</p>
      <h2 id="what-it-is-not" class="group scroll-mt-20">What it is not<a href="#what-it-is-not" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>It is not a general-purpose text editor, and it is not a CMS. It edits files on disk, in a folder you control, under whatever version control you already use.</p>
      <h2 id="where-to-start" class="group scroll-mt-20">Where to start<a href="#where-to-start" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./install" class="[text-decoration:none] text-foreground">
      <Icon name="download" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Installing</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Packages for Linux, macOS and Windows, or build from source.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./tutorial" class="[text-decoration:none] text-foreground">
      <Icon name="rocket" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Tutorial: fixing a DITA guide</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Thirty minutes with a real, deliberately broken project.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

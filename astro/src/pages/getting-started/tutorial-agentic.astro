---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: getting-started/tutorial-agentic.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Step from "@ui/steps/Step.astro";
import Steps from "@ui/steps/Steps.astro";

const headings = [{"depth":1,"slug":"tutorial-using-an-agent-to-fix-a-dita-project","text":"Tutorial: Using an agent to fix a DITA project"},{"depth":2,"slug":"step-1-open-the-sample-project","text":"Step 1: Open the sample project"},{"depth":2,"slug":"step-2-open-the-agent","text":"Step 2: Open the agent"},{"depth":2,"slug":"step-3-ask-it-to-find-and-fix-the-problems","text":"Step 3: Ask it to find and fix the problems"},{"depth":2,"slug":"step-4-review-what-it-proposes","text":"Step 4: Review what it proposes"},{"depth":2,"slug":"step-5-check-the-project-again","text":"Step 5: Check the project again"},{"depth":2,"slug":"what-you-learned","text":"What you learned"},{"depth":2,"slug":"where-to-go-next","text":"Where to go next"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/getting-started/tutorial-agentic";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Tutorial: Using an agent to fix a DITA project";
const description = "Open a real DITA project that has problems in it, find them with the project tools, and fix them with the AI agent." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "tutorial";
const pageAudience = null;
const pageCategory = ["getting-started"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Tutorial: Using an agent to fix a DITA project {#tutorial-using-an-agent-to-fix-a-dita-project}\n\nIn this tutorial you inherit a documentation set that does not build cleanly, find out what is wrong with it, and fix part of it with the AI agent.\n\nThe project is a DITA user guide for the Audacity audio editor. It ships with the editor, and it is deliberately broken: topics without descriptions, links that point nowhere, a hardcoded product name that should be a key, and conditions that the subject scheme disallows.\n\n**Time:** about 30 minutes. **You need:** DogsBay XML installed. See [Installing](./install). A subscription or API key for the agent section is useful but not required until then.\n\n## Step 1: Open the sample project {#step-1-open-the-sample-project}\n\n:::steps\n1. **Select File > Open Sample Project** The editor asks where to put it.\n2. **Choose a location** The sample is copied into a new `audacity-demo` folder inside the location you choose. It never uses the folder you chose as the project itself, and it never overwrites anything: if `audacity-demo` exists, the copy becomes `audacity-demo-2`.\n3. **Wait for the project to open** The Topic Map explorer loads `audacity-guide.ditamap`, and publishing is configured, without you setting anything up.\n:::\n\nYou now have your own copy to break further and repair.\n\n## Step 2: Open the agent {#step-2-open-the-agent}\n\n:::steps\n1. **Open the AI Agent panel** It is on the right-hand sidebar. The first tab is the built-in agent, which runs as you and needs no integration server.\n2. **Choose a provider and sign in** Use the provider bar at the top of the panel. The key is stored in your operating system keychain, not in the project.\n:::\n\nIf you would rather drive Claude Code, Codex or Gemini from the panel instead, see [using a hosted agent](/dogsbay-xml-docs/agent/hosted-agents). A hosted session opens at tier T1 and needs the integration server; the built-in agent needs neither.\n\n## Step 3: Ask it to find and fix the problems {#step-3-ask-it-to-find-and-fix-the-problems}\n\nFixing thirty topics by hand is the wrong tool. This is what the agent is for.\n\n:::steps\n1. **Ask for the survey first** Something like: *Run a project health check on this project, applying house-style.sch, and summarise what is wrong, grouped by kind.* The agent calls the same `project-health` operation the command line does, so the answer is the project's, not a guess from reading a few files. Naming the schema matters: the missing shortdescs are a house rule, and the check only applies one when it is given.\n2. **Pick one kind of problem** *Add a one-sentence shortdesc to every topic that has none.* Naming one kind at a time keeps the review in the next step manageable.\n3. **Watch the panel while it works** Each operation it calls is listed as it runs, so you can see it is calling the editor's DITA commands rather than rewriting files as text.\n:::\n\nThe agent works across the project rather than opening each file in turn, because it has the editor's project operations available to it.\n\n## Step 4: Review what it proposes {#step-4-review-what-it-proposes}\n\nThe agent's edits do not overwrite your files. They arrive as tracked changes.\n\n:::steps\n1. **Open the Proposals panel** Each proposed change is listed with the identity that made it.\n2. **Select one** The editor jumps to the affected part of the document and shows the change in place.\n3. **Accept or reject it** Use **Accept** or **Reject** for one change, or **Accept all** and **Reject all** when you have read enough to trust the batch.\n:::\n\nThis is the part worth slowing down for. The agent proposes; you decide.\n\n## Step 5: Check the project again {#step-5-check-the-project-again}\n\n```bash\ndogsbay-xml project-health . --schematron house-style.sch\n```\n\nFewer problems should remain. What is left is the more interesting work: the hardcoded product name that should be a key, the conditions that are not in the subject scheme, and the topic that is too long and should be split.\n\n## What you learned {#what-you-learned}\n\n- The sample project is a copy, so you can experiment freely.\n- Problems are found at project level, not file by file.\n- The agent calls the editor's own DITA operations rather than editing text.\n- Its changes are reviewable before they land: it proposes, you decide.\n\n## Where to go next {#where-to-go-next}\n\n:::cards\n- **[The agent, and what it may touch](/dogsbay-xml-docs/agent/overview){icon=\"sparkles\"}**\n  Tiers, the write gate, and the audit log.\n\n- **[Reviewing an agent's changes](/dogsbay-xml-docs/agent/proposals){icon=\"check\"}**\n  Proposals in more detail, including comments.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/getting-started/tutorial-agentic.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="tutorial-using-an-agent-to-fix-a-dita-project" class="group scroll-mt-20">Tutorial: Using an agent to fix a DITA project<a href="#tutorial-using-an-agent-to-fix-a-dita-project" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>In this tutorial you inherit a documentation set that does not build cleanly, find out what is wrong with it, and fix part of it with the AI agent.</p>
      <p>The project is a DITA user guide for the Audacity audio editor. It ships with the editor, and it is deliberately broken: topics without descriptions, links that point nowhere, a hardcoded product name that should be a key, and conditions that the subject scheme disallows.</p>
      <p><strong>Time:</strong> about 30 minutes. <strong>You need:</strong> DogsBay XML installed. See <a href="./install">Installing</a>. A subscription or API key for the agent section is useful but not required until then.</p>
      <h2 id="step-1-open-the-sample-project" class="group scroll-mt-20">Step 1: Open the sample project<a href="#step-1-open-the-sample-project" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Steps>
      <Step>
      <strong>Select File &gt; Open Sample Project</strong> The editor asks where to put it.
      </Step>
      <Step>
      <strong>Choose a location</strong> The sample is copied into a new <code>audacity-demo</code> folder inside the location you choose. It never uses the folder you chose as the project itself, and it never overwrites anything: if <code>audacity-demo</code> exists, the copy becomes <code>audacity-demo-2</code>.
      </Step>
      <Step>
      <strong>Wait for the project to open</strong> The Topic Map explorer loads <code>audacity-guide.ditamap</code>, and publishing is configured, without you setting anything up.
      </Step>
      </Steps>
      <p>You now have your own copy to break further and repair.</p>
      <h2 id="step-2-open-the-agent" class="group scroll-mt-20">Step 2: Open the agent<a href="#step-2-open-the-agent" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Steps>
      <Step>
      <strong>Open the AI Agent panel</strong> It is on the right-hand sidebar. The first tab is the built-in agent, which runs as you and needs no integration server.
      </Step>
      <Step>
      <strong>Choose a provider and sign in</strong> Use the provider bar at the top of the panel. The key is stored in your operating system keychain, not in the project.
      </Step>
      </Steps>
      <p>If you would rather drive Claude Code, Codex or Gemini from the panel instead, see <a href="/dogsbay-xml-docs/agent/hosted-agents">using a hosted agent</a>. A hosted session opens at tier T1 and needs the integration server; the built-in agent needs neither.</p>
      <h2 id="step-3-ask-it-to-find-and-fix-the-problems" class="group scroll-mt-20">Step 3: Ask it to find and fix the problems<a href="#step-3-ask-it-to-find-and-fix-the-problems" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Fixing thirty topics by hand is the wrong tool. This is what the agent is for.</p>
      <Steps>
      <Step>
      <strong>Ask for the survey first</strong> Something like: <em>Run a project health check on this project, applying house-style.sch, and summarise what is wrong, grouped by kind.</em> The agent calls the same <code>project-health</code> operation the command line does, so the answer is the project's, not a guess from reading a few files. Naming the schema matters: the missing shortdescs are a house rule, and the check only applies one when it is given.
      </Step>
      <Step>
      <strong>Pick one kind of problem</strong> <em>Add a one-sentence shortdesc to every topic that has none.</em> Naming one kind at a time keeps the review in the next step manageable.
      </Step>
      <Step>
      <strong>Watch the panel while it works</strong> Each operation it calls is listed as it runs, so you can see it is calling the editor's DITA commands rather than rewriting files as text.
      </Step>
      </Steps>
      <p>The agent works across the project rather than opening each file in turn, because it has the editor's project operations available to it.</p>
      <h2 id="step-4-review-what-it-proposes" class="group scroll-mt-20">Step 4: Review what it proposes<a href="#step-4-review-what-it-proposes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The agent's edits do not overwrite your files. They arrive as tracked changes.</p>
      <Steps>
      <Step>
      <strong>Open the Proposals panel</strong> Each proposed change is listed with the identity that made it.
      </Step>
      <Step>
      <strong>Select one</strong> The editor jumps to the affected part of the document and shows the change in place.
      </Step>
      <Step>
      <strong>Accept or reject it</strong> Use <strong>Accept</strong> or <strong>Reject</strong> for one change, or <strong>Accept all</strong> and <strong>Reject all</strong> when you have read enough to trust the batch.
      </Step>
      </Steps>
      <p>This is the part worth slowing down for. The agent proposes; you decide.</p>
      <h2 id="step-5-check-the-project-again" class="group scroll-mt-20">Step 5: Check the project again<a href="#step-5-check-the-project-again" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <CodeBlock code={"dogsbay-xml project-health . --schematron house-style.sch"} lang="bash" />
      <p>Fewer problems should remain. What is left is the more interesting work: the hardcoded product name that should be a key, the conditions that are not in the subject scheme, and the topic that is too long and should be split.</p>
      <h2 id="what-you-learned" class="group scroll-mt-20">What you learned<a href="#what-you-learned" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li>The sample project is a copy, so you can experiment freely.</li>
      <li>Problems are found at project level, not file by file.</li>
      <li>The agent calls the editor's own DITA operations rather than editing text.</li>
      <li>Its changes are reviewable before they land: it proposes, you decide.</li>
      </ul>
      <h2 id="where-to-go-next" class="group scroll-mt-20">Where to go next<a href="#where-to-go-next" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/agent/overview" class="[text-decoration:none] text-foreground">
      <Icon name="sparkles" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">The agent, and what it may touch</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Tiers, the write gate, and the audit log.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/agent/proposals" class="[text-decoration:none] text-foreground">
      <Icon name="check" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Reviewing an agent's changes</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Proposals in more detail, including comments.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

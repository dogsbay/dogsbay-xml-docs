---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: automation/mcp.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Step from "@ui/steps/Step.astro";
import Steps from "@ui/steps/Steps.astro";

const headings = [{"depth":1,"slug":"connecting-an-ai-assistant","text":"Connecting an AI assistant"},{"depth":2,"slug":"turn-on-the-server","text":"Turn on the server"},{"depth":2,"slug":"connect-a-client","text":"Connect a client"},{"depth":2,"slug":"what-the-assistant-can-do","text":"What the assistant can do"},{"depth":2,"slug":"if-the-token-changes","text":"If the token changes"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/automation/mcp";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Connecting an AI assistant";
const description = "Provide the editor's DITA operations to Claude Code, Claude Desktop, or Cursor over MCP." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "how-to";
const pageAudience = null;
const pageCategory = ["automation"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Connecting an AI assistant {#connecting-an-ai-assistant}\n\nThe editor can provide its operations to an AI assistant over the Model Context Protocol. The assistant then works on your project through the same commands the editor and the command line use, rather than by reading and rewriting files as text.\n\nThis setup differs from using an agent inside the editor. The assistant runs in a terminal or desktop application, and the editor provides the tools.\n\n## Turn on the server {#turn-on-the-server}\n\nThe integration server is off by default.\n\n:::steps\n1. **Open the settings** Select **File > Settings**.\n2. **Enable the server** Select **Server**, and then enable the server.\n3. **Confirm it is listening**\n\n   ```bash\n   dogsbay-xml status\n   ```\n:::\n\nThe server binds to localhost and requires a token, which is written to a discovery file in your home directory so local clients can find it.\n\n## Connect a client {#connect-a-client}\n\nThe editor prints the configuration for you:\n\n```bash\ndogsbay-xml status --mcp-config\n```\n\nFor Claude Code, it can print the command that registers it:\n\n```bash\ndogsbay-xml status --mcp-install\n```\n\nPaste the configuration into the client's MCP settings, or run the command.\n\n## What the assistant can do {#what-the-assistant-can-do}\n\nThe tools support the same operations as the command line:\n\n- **Read and validate** documents, including Schematron and project-wide validation\n- **Inspect the project** to find where a file is used, what a key resolves to, which references are broken, and the project's overall health\n- **Refactor** by renaming files, keys, and IDs; converting between direct references and keys; extracting and inlining reuse; and splitting topics\n- **Edit** the structure of maps and relationship tables\n- **Control the editor** by opening documents, moving the cursor, selecting elements, and reading the outline\n- **Review** proposals, add comments, and read the audit log\n\nEvery call is attributed to a session, so the audit log records which assistant did what.\n\n## If the token changes {#if-the-token-changes}\n\nRegenerating the token invalidates any client configured with the old one:\n\n```bash\ndogsbay-xml status --reset-token\n```\n\nRestart the editor, and give clients the new configuration.\n\n## Related {#related}\n\n:::cards\n- **[The agent, and what it may touch](/dogsbay-xml-docs/agent/overview){icon=\"shield\"}**\n  Tiers, the write gate, and the audit log, which also apply here.\n\n- **[The command line](/dogsbay-xml-docs/reference/cli){icon=\"terminal\"}**\n  The same operations, without a client.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/automation/mcp.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  pagefindBaseUrl={"/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="connecting-an-ai-assistant" class="group scroll-mt-20">Connecting an AI assistant<a href="#connecting-an-ai-assistant" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h1>
      <p>The editor can provide its operations to an AI assistant over the Model Context Protocol. The assistant then works on your project through the same commands the editor and the command line use, rather than by reading and rewriting files as text.</p>
      <p>This setup differs from using an agent inside the editor. The assistant runs in a terminal or desktop application, and the editor provides the tools.</p>
      <h2 id="turn-on-the-server" class="group scroll-mt-20">Turn on the server<a href="#turn-on-the-server" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>The integration server is off by default.</p>
      <Steps>
      <Step>
      <strong>Open the settings</strong> Select <strong>File &gt; Settings</strong>.
      </Step>
      <Step>
      <strong>Enable the server</strong> Select <strong>Server</strong>, and then enable the server.
      </Step>
      <Step>
      <strong>Confirm it is listening</strong>
      <CodeBlock code={"dogsbay-xml status"} lang="bash" />
      </Step>
      </Steps>
      <p>The server binds to localhost and requires a token, which is written to a discovery file in your home directory so local clients can find it.</p>
      <h2 id="connect-a-client" class="group scroll-mt-20">Connect a client<a href="#connect-a-client" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>The editor prints the configuration for you:</p>
      <CodeBlock code={"dogsbay-xml status --mcp-config"} lang="bash" />
      <p>For Claude Code, it can print the command that registers it:</p>
      <CodeBlock code={"dogsbay-xml status --mcp-install"} lang="bash" />
      <p>Paste the configuration into the client's MCP settings, or run the command.</p>
      <h2 id="what-the-assistant-can-do" class="group scroll-mt-20">What the assistant can do<a href="#what-the-assistant-can-do" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>The tools support the same operations as the command line:</p>
      <ul>
      <li><strong>Read and validate</strong> documents, including Schematron and project-wide validation</li>
      <li><strong>Inspect the project</strong> to find where a file is used, what a key resolves to, which references are broken, and the project's overall health</li>
      <li><strong>Refactor</strong> by renaming files, keys, and IDs; converting between direct references and keys; extracting and inlining reuse; and splitting topics</li>
      <li><strong>Edit</strong> the structure of maps and relationship tables</li>
      <li><strong>Control the editor</strong> by opening documents, moving the cursor, selecting elements, and reading the outline</li>
      <li><strong>Review</strong> proposals, add comments, and read the audit log</li>
      </ul>
      <p>Every call is attributed to a session, so the audit log records which assistant did what.</p>
      <h2 id="if-the-token-changes" class="group scroll-mt-20">If the token changes<a href="#if-the-token-changes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>Regenerating the token invalidates any client configured with the old one:</p>
      <CodeBlock code={"dogsbay-xml status --reset-token"} lang="bash" />
      <p>Restart the editor, and give clients the new configuration.</p>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/agent/overview" class="[text-decoration:none] text-foreground">
      <Icon name="shield" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">The agent, and what it may touch</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Tiers, the write gate, and the audit log, which also apply here.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/reference/cli" class="[text-decoration:none] text-foreground">
      <Icon name="terminal" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">The command line</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">The same operations, without a client.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: reference/project-config.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"project-configuration","text":"Project configuration"},{"depth":2,"slug":"the-dogsbay-folder","text":"The .dogsbay folder"},{"depth":2,"slug":"creating-the-configuration","text":"Creating the configuration"},{"depth":2,"slug":"configxml","text":"config.xml"},{"depth":3,"slug":"metadata-policy","text":"Metadata policy"},{"depth":3,"slug":"format-style","text":"Format style"},{"depth":2,"slug":"localxml","text":"local.xml"},{"depth":2,"slug":"how-the-dita-ot-is-found","text":"How the DITA-OT is found"},{"depth":2,"slug":"example","text":"Example"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/reference/project-config";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Project configuration";
const description = "The files in a project's .dogsbay folder, what each element means, and which ones to commit." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "reference";
const pageAudience = null;
const pageCategory = ["reference"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Project configuration {#project-configuration}\n\nA project keeps its own configuration in a `.dogsbay` folder at the root of the project. The settings there travel with the content, so everyone who clones the project gets the same root map, framework, metadata policy and formatting. Settings that apply to every project, such as the editor theme and key bindings, are described in the [settings reference](/dogsbay-xml-docs/reference/settings).\n\n## The .dogsbay folder {#the-dogsbay-folder}\n\n| File | Shared | Holds |\n| --- | --- | --- |\n| `config.xml` | Yes, commit it | The project type, default root map, framework, default deliverable, metadata policy, formatting house style, and whether to format on save. |\n| `local.xml` | No | Your personal overrides for this machine: a DITA-OT path and the deliverable you have selected. |\n| `.gitignore` | Yes, commit it | Created with `config.xml`. It lists `local.xml` so personal settings stay out of the repository. |\n\nEvery file is optional. A missing file, or one that is not well-formed XML, is treated as if it were empty, and the editor falls back to what it can detect.\n\n## Creating the configuration {#creating-the-configuration}\n\nWhen you open a folder that has no `config.xml` and the editor recognizes it as a DITA project, it writes a `config.xml` with the project type, the root map it detected, and the framework it chose. Commit the file if the choices are right, or change them and save again.\n\nThese actions write to `config.xml` and keep the parts they do not change:\n\n| Action | Writes |\n| --- | --- |\n| **Project > Save Project Settings...** | The project type, framework, and default root map, and the selected deliverable as the default deliverable. |\n| **Project > Metadata > Edit Policy...** | The metadata policy. |\n| **Save to project (.dogsbay/config.xml)...** on the [Format](/dogsbay-xml-docs/reference/settings#format) settings page | The formatting house style and the format-on-save setting. |\n\nSelecting a deliverable writes it to `local.xml`. You can also edit either file by hand. After editing by hand, open the project folder again so the editor picks up every change.\n\n## config.xml {#configxml}\n\nThe root element is `dogsbay-project`. Each child element is optional.\n\n| Element | Content | Meaning |\n| --- | --- | --- |\n| `project-type` | `DITA`, `DocBook` or `None` | The kind of content in the project. |\n| `default-root-map` | A path relative to the project root | The map used for validation, key resolution, and publishing. |\n| `framework` | A framework name, such as `DITA-OT 4.3.5` | The framework the project requires, by name. Each person's editor finds its own installed framework with that name, so the file contains no machine paths. |\n| `default-deliverable` | Attributes `file` and `name` | The deliverable selected when someone opens the project for the first time. `file` is the DITA-OT project file, relative to the project root, and `name` is a deliverable defined in it. |\n| `metadata-policy` | `rule` elements | The metadata that topics must contain. See [Metadata policy](#metadata-policy). |\n| `format-style` | Attributes, and optional `preserve-space` elements | The formatting house style. See [Format style](#format-style). |\n| `format-on-save` | `true` | Reformats documents when they are saved. When the element is absent, format on save is off. |\n\nThe deliverables themselves are defined in the DITA-OT project file (`project.json`, `project.xml` or `project.yaml`), not in `config.xml`.\n\n### Metadata policy {#metadata-policy}\n\nEach `rule` element in `metadata-policy` requires, recommends or forbids one metadata field. The [metadata audit](/dogsbay-xml-docs/authoring/metadata) checks topics against these rules.\n\n| Attribute | Values | Default | Meaning |\n| --- | --- | --- | --- |\n| `field` | A field name from the table below | Required | The metadata field the rule checks. A rule with an unknown field is ignored. |\n| `presence` | `required`, `recommended` or `forbidden` | `required` | Whether the field must be present, should be present, or must not be present. |\n| `topic-type` | A topic type, such as `task` or `concept` | Any type | Limits the rule to topics of that type. |\n| `allowed-values` | Values separated by spaces | Any value | The only values the field may have. |\n| `pattern` | A regular expression | Any value | A pattern that the value must match. |\n\nFor an allowed value that contains spaces, add a `value` child element for it instead of using `allowed-values`. You can use both together.\n\nThe field names are `author`, `source`, `publisher`, `copyryear`, `copyrholder`, `created`, `created-golive`, `created-expiry`, `revised`, `revised-golive`, `revised-expiry`, `permissions`, `audience`, `audience-job`, `audience-experiencelevel`, `category`, `keyword`, `indexterm`, `prodname`, and `vrm-version`. A name with a suffix checks an attribute of the element: for example, `created` checks the `date` attribute of `<created>` and `created-golive` checks its `golive` attribute.\n\n### Format style {#format-style}\n\nThe `format-style` attributes set the house style used by **Format Document**, by format on save, and by the `dogsbay-xml format` command. An attribute that is missing takes its default value.\n\n| Attribute | Values | Default | Meaning |\n| --- | --- | --- | --- |\n| `indent` | `spaces` or `tabs` | `spaces` | The character used for one level of indentation. |\n| `size` | A number | `2` | Spaces per indentation level, or the display width of a tab. |\n| `max-line-width` | A number | `0` | The column at which prose is wrapped. `0` never wraps prose, which keeps diffs small. |\n| `preserve-mixed` | `true` or `false` | `true` | Keeps elements that mix text and markup, such as a paragraph with inline elements, on their existing lines. |\n| `newline` | `lf`, `crlf` or `platform` | `lf` | The line ending. `platform` uses the line ending of the computer doing the formatting. |\n| `final-newline` | `true` or `false` | `true` | Ends the file with a newline. |\n| `preserve-text-breaks` | `true` or `false` | `true` | Keeps the line breaks you typed in prose, so one sentence per line survives formatting. |\n| `preserve-blank-lines` | `true` or `false` | `true` | Keeps one blank line where you left blank lines, and removes the extras. |\n| `text-continuation` | `block` or `flush` | `block` | Where the continuation lines of wrapped text start. `block` aligns them with the element's indentation, and `flush` starts them at the first column, so moving an element does not re-indent its text. |\n| `trim-whitespace` | `true` or `false` | `true` | Removes whitespace at the ends of lines. |\n\nThe contents of `codeblock`, `pre`, `lines` and `screen` elements are never reformatted. To protect other elements in the same way, add a `preserve-space` child element for each one, containing the element name.\n\nIf a project has no `format-style`, the editor uses the style on the [Format](/dogsbay-xml-docs/reference/settings#format) settings page.\n\n## local.xml {#localxml}\n\nThe root element is `dogsbay-project-local`. The file is ignored by Git.\n\n| Element | Content | Meaning |\n| --- | --- | --- |\n| `dita-ot-path` | An absolute path | The DITA-OT installation to use for this project on this machine. It overrides the framework named in `config.xml`. |\n| `active-deliverable` | Attributes `file` and `name` | The deliverable you last selected. It overrides `default-deliverable` for you. |\n\n## How the DITA-OT is found {#how-the-dita-ot-is-found}\n\nThe editor uses the first DITA-OT installation that it finds in this order:\n\n1. The `dita-ot-path` in `local.xml`.\n2. The installed framework whose name matches `framework` in `config.xml`.\n3. The first installed framework that has a DITA-OT path.\n\n## Example {#example}\n\nThis is the `config.xml` from the sample project:\n\n```xml\n<dogsbay-project>\n  <project-type>DITA</project-type>\n  <default-root-map>audacity-guide.ditamap</default-root-map>\n  <framework>DITA-OT 4.3.5</framework>\n  <default-deliverable file=\"project.json\" name=\"full\"/>\n  <metadata-policy>\n    <rule topic-type=\"task\" field=\"created\" presence=\"required\" pattern=\"\\d{4}-\\d\\d-\\d\\d\"/>\n    <rule field=\"author\" presence=\"recommended\"/>\n    <rule field=\"keyword\" presence=\"required\"/>\n  </metadata-policy>\n  <format-style indent=\"spaces\" size=\"2\" max-line-width=\"0\" preserve-mixed=\"true\" newline=\"lf\" final-newline=\"true\" preserve-text-breaks=\"true\" preserve-blank-lines=\"true\" text-continuation=\"block\" trim-whitespace=\"true\"/>\n</dogsbay-project>\n```\n\nThe policy requires every task to record a creation date in the form `2026-01-31`, requires a keyword in every topic, and recommends an author. Format on save is off because the file has no `format-on-save` element.\n\n## Related {#related}\n\n- [Settings reference](/dogsbay-xml-docs/reference/settings)\n- [Metadata](/dogsbay-xml-docs/authoring/metadata)\n- [The command line](/dogsbay-xml-docs/reference/cli)\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/reference/project-config.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  pagefindBaseUrl={"/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="project-configuration" class="group scroll-mt-20">Project configuration<a href="#project-configuration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h1>
      <p>A project keeps its own configuration in a <code>.dogsbay</code> folder at the root of the project. The settings there travel with the content, so everyone who clones the project gets the same root map, framework, metadata policy and formatting. Settings that apply to every project, such as the editor theme and key bindings, are described in the <a href="/dogsbay-xml-docs/reference/settings">settings reference</a>.</p>
      <h2 id="the-dogsbay-folder" class="group scroll-mt-20">The .dogsbay folder<a href="#the-dogsbay-folder" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>File</TableHead>
      <TableHead>Shared</TableHead>
      <TableHead>Holds</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>config.xml</code></TableCell>
      <TableCell>Yes, commit it</TableCell>
      <TableCell>The project type, default root map, framework, default deliverable, metadata policy, formatting house style, and whether to format on save.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>local.xml</code></TableCell>
      <TableCell>No</TableCell>
      <TableCell>Your personal overrides for this machine: a DITA-OT path and the deliverable you have selected.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>.gitignore</code></TableCell>
      <TableCell>Yes, commit it</TableCell>
      <TableCell>Created with <code>config.xml</code>. It lists <code>local.xml</code> so personal settings stay out of the repository.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Every file is optional. A missing file, or one that is not well-formed XML, is treated as if it were empty, and the editor falls back to what it can detect.</p>
      <h2 id="creating-the-configuration" class="group scroll-mt-20">Creating the configuration<a href="#creating-the-configuration" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>When you open a folder that has no <code>config.xml</code> and the editor recognizes it as a DITA project, it writes a <code>config.xml</code> with the project type, the root map it detected, and the framework it chose. Commit the file if the choices are right, or change them and save again.</p>
      <p>These actions write to <code>config.xml</code> and keep the parts they do not change:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Action</TableHead>
      <TableHead>Writes</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><strong>Project &gt; Save Project Settings...</strong></TableCell>
      <TableCell>The project type, framework, and default root map, and the selected deliverable as the default deliverable.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Project &gt; Metadata &gt; Edit Policy...</strong></TableCell>
      <TableCell>The metadata policy.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><strong>Save to project (.dogsbay/config.xml)...</strong> on the <a href="/dogsbay-xml-docs/reference/settings#format">Format</a> settings page</TableCell>
      <TableCell>The formatting house style and the format-on-save setting.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Selecting a deliverable writes it to <code>local.xml</code>. You can also edit either file by hand. After editing by hand, open the project folder again so the editor picks up every change.</p>
      <h2 id="configxml" class="group scroll-mt-20">config.xml<a href="#configxml" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>The root element is <code>dogsbay-project</code>. Each child element is optional.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Element</TableHead>
      <TableHead>Content</TableHead>
      <TableHead>Meaning</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>project-type</code></TableCell>
      <TableCell><code>DITA</code>, <code>DocBook</code> or <code>None</code></TableCell>
      <TableCell>The kind of content in the project.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>default-root-map</code></TableCell>
      <TableCell>A path relative to the project root</TableCell>
      <TableCell>The map used for validation, key resolution, and publishing.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>framework</code></TableCell>
      <TableCell>A framework name, such as <code>DITA-OT 4.3.5</code></TableCell>
      <TableCell>The framework the project requires, by name. Each person's editor finds its own installed framework with that name, so the file contains no machine paths.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>default-deliverable</code></TableCell>
      <TableCell>Attributes <code>file</code> and <code>name</code></TableCell>
      <TableCell>The deliverable selected when someone opens the project for the first time. <code>file</code> is the DITA-OT project file, relative to the project root, and <code>name</code> is a deliverable defined in it.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>metadata-policy</code></TableCell>
      <TableCell><code>rule</code> elements</TableCell>
      <TableCell>The metadata that topics must contain. See <a href="#metadata-policy">Metadata policy</a>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>format-style</code></TableCell>
      <TableCell>Attributes, and optional <code>preserve-space</code> elements</TableCell>
      <TableCell>The formatting house style. See <a href="#format-style">Format style</a>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>format-on-save</code></TableCell>
      <TableCell><code>true</code></TableCell>
      <TableCell>Reformats documents when they are saved. When the element is absent, format on save is off.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>The deliverables themselves are defined in the DITA-OT project file (<code>project.json</code>, <code>project.xml</code> or <code>project.yaml</code>), not in <code>config.xml</code>.</p>
      <h3 id="metadata-policy" class="group scroll-mt-20">Metadata policy<a href="#metadata-policy" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <p>Each <code>rule</code> element in <code>metadata-policy</code> requires, recommends or forbids one metadata field. The <a href="/dogsbay-xml-docs/authoring/metadata">metadata audit</a> checks topics against these rules.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Attribute</TableHead>
      <TableHead>Values</TableHead>
      <TableHead>Default</TableHead>
      <TableHead>Meaning</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>field</code></TableCell>
      <TableCell>A field name from the table below</TableCell>
      <TableCell>Required</TableCell>
      <TableCell>The metadata field the rule checks. A rule with an unknown field is ignored.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>presence</code></TableCell>
      <TableCell><code>required</code>, <code>recommended</code> or <code>forbidden</code></TableCell>
      <TableCell><code>required</code></TableCell>
      <TableCell>Whether the field must be present, should be present, or must not be present.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>topic-type</code></TableCell>
      <TableCell>A topic type, such as <code>task</code> or <code>concept</code></TableCell>
      <TableCell>Any type</TableCell>
      <TableCell>Limits the rule to topics of that type.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>allowed-values</code></TableCell>
      <TableCell>Values separated by spaces</TableCell>
      <TableCell>Any value</TableCell>
      <TableCell>The only values the field may have.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>pattern</code></TableCell>
      <TableCell>A regular expression</TableCell>
      <TableCell>Any value</TableCell>
      <TableCell>A pattern that the value must match.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>For an allowed value that contains spaces, add a <code>value</code> child element for it instead of using <code>allowed-values</code>. You can use both together.</p>
      <p>The field names are <code>author</code>, <code>source</code>, <code>publisher</code>, <code>copyryear</code>, <code>copyrholder</code>, <code>created</code>, <code>created-golive</code>, <code>created-expiry</code>, <code>revised</code>, <code>revised-golive</code>, <code>revised-expiry</code>, <code>permissions</code>, <code>audience</code>, <code>audience-job</code>, <code>audience-experiencelevel</code>, <code>category</code>, <code>keyword</code>, <code>indexterm</code>, <code>prodname</code>, and <code>vrm-version</code>. A name with a suffix checks an attribute of the element: for example, <code>created</code> checks the <code>date</code> attribute of <code>&lt;created&gt;</code> and <code>created-golive</code> checks its <code>golive</code> attribute.</p>
      <h3 id="format-style" class="group scroll-mt-20">Format style<a href="#format-style" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <p>The <code>format-style</code> attributes set the house style used by <strong>Format Document</strong>, by format on save, and by the <code>dogsbay-xml format</code> command. An attribute that is missing takes its default value.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Attribute</TableHead>
      <TableHead>Values</TableHead>
      <TableHead>Default</TableHead>
      <TableHead>Meaning</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>indent</code></TableCell>
      <TableCell><code>spaces</code> or <code>tabs</code></TableCell>
      <TableCell><code>spaces</code></TableCell>
      <TableCell>The character used for one level of indentation.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>size</code></TableCell>
      <TableCell>A number</TableCell>
      <TableCell><code>2</code></TableCell>
      <TableCell>Spaces per indentation level, or the display width of a tab.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>max-line-width</code></TableCell>
      <TableCell>A number</TableCell>
      <TableCell><code>0</code></TableCell>
      <TableCell>The column at which prose is wrapped. <code>0</code> never wraps prose, which keeps diffs small.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>preserve-mixed</code></TableCell>
      <TableCell><code>true</code> or <code>false</code></TableCell>
      <TableCell><code>true</code></TableCell>
      <TableCell>Keeps elements that mix text and markup, such as a paragraph with inline elements, on their existing lines.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>newline</code></TableCell>
      <TableCell><code>lf</code>, <code>crlf</code> or <code>platform</code></TableCell>
      <TableCell><code>lf</code></TableCell>
      <TableCell>The line ending. <code>platform</code> uses the line ending of the computer doing the formatting.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>final-newline</code></TableCell>
      <TableCell><code>true</code> or <code>false</code></TableCell>
      <TableCell><code>true</code></TableCell>
      <TableCell>Ends the file with a newline.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>preserve-text-breaks</code></TableCell>
      <TableCell><code>true</code> or <code>false</code></TableCell>
      <TableCell><code>true</code></TableCell>
      <TableCell>Keeps the line breaks you typed in prose, so one sentence per line survives formatting.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>preserve-blank-lines</code></TableCell>
      <TableCell><code>true</code> or <code>false</code></TableCell>
      <TableCell><code>true</code></TableCell>
      <TableCell>Keeps one blank line where you left blank lines, and removes the extras.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>text-continuation</code></TableCell>
      <TableCell><code>block</code> or <code>flush</code></TableCell>
      <TableCell><code>block</code></TableCell>
      <TableCell>Where the continuation lines of wrapped text start. <code>block</code> aligns them with the element's indentation, and <code>flush</code> starts them at the first column, so moving an element does not re-indent its text.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>trim-whitespace</code></TableCell>
      <TableCell><code>true</code> or <code>false</code></TableCell>
      <TableCell><code>true</code></TableCell>
      <TableCell>Removes whitespace at the ends of lines.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>The contents of <code>codeblock</code>, <code>pre</code>, <code>lines</code> and <code>screen</code> elements are never reformatted. To protect other elements in the same way, add a <code>preserve-space</code> child element for each one, containing the element name.</p>
      <p>If a project has no <code>format-style</code>, the editor uses the style on the <a href="/dogsbay-xml-docs/reference/settings#format">Format</a> settings page.</p>
      <h2 id="localxml" class="group scroll-mt-20">local.xml<a href="#localxml" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>The root element is <code>dogsbay-project-local</code>. The file is ignored by Git.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Element</TableHead>
      <TableHead>Content</TableHead>
      <TableHead>Meaning</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>dita-ot-path</code></TableCell>
      <TableCell>An absolute path</TableCell>
      <TableCell>The DITA-OT installation to use for this project on this machine. It overrides the framework named in <code>config.xml</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>active-deliverable</code></TableCell>
      <TableCell>Attributes <code>file</code> and <code>name</code></TableCell>
      <TableCell>The deliverable you last selected. It overrides <code>default-deliverable</code> for you.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="how-the-dita-ot-is-found" class="group scroll-mt-20">How the DITA-OT is found<a href="#how-the-dita-ot-is-found" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>The editor uses the first DITA-OT installation that it finds in this order:</p>
      <ol>
      <li>The <code>dita-ot-path</code> in <code>local.xml</code>.</li>
      <li>The installed framework whose name matches <code>framework</code> in <code>config.xml</code>.</li>
      <li>The first installed framework that has a DITA-OT path.</li>
      </ol>
      <h2 id="example" class="group scroll-mt-20">Example<a href="#example" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>This is the <code>config.xml</code> from the sample project:</p>
      <CodeBlock code={"<dogsbay-project>\n  <project-type>DITA</project-type>\n  <default-root-map>audacity-guide.ditamap</default-root-map>\n  <framework>DITA-OT 4.3.5</framework>\n  <default-deliverable file=\"project.json\" name=\"full\"/>\n  <metadata-policy>\n    <rule topic-type=\"task\" field=\"created\" presence=\"required\" pattern=\"\\d{4}-\\d\\d-\\d\\d\"/>\n    <rule field=\"author\" presence=\"recommended\"/>\n    <rule field=\"keyword\" presence=\"required\"/>\n  </metadata-policy>\n  <format-style indent=\"spaces\" size=\"2\" max-line-width=\"0\" preserve-mixed=\"true\" newline=\"lf\" final-newline=\"true\" preserve-text-breaks=\"true\" preserve-blank-lines=\"true\" text-continuation=\"block\" trim-whitespace=\"true\"/>\n</dogsbay-project>"} lang="xml" />
      <p>The policy requires every task to record a creation date in the form <code>2026-01-31</code>, requires a keyword in every topic, and recommends an author. Format on save is off because the file has no <code>format-on-save</code> element.</p>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <ul>
      <li><a href="/dogsbay-xml-docs/reference/settings">Settings reference</a></li>
      <li><a href="/dogsbay-xml-docs/authoring/metadata">Metadata</a></li>
      <li><a href="/dogsbay-xml-docs/reference/cli">The command line</a></li>
      </ul>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

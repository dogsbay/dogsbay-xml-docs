---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: reference/project-graph.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"project-graph","text":"Project graph"},{"depth":2,"slug":"options","text":"Options"},{"depth":2,"slug":"the-result","text":"The result"},{"depth":3,"slug":"deliverables","text":"Deliverables"},{"depth":3,"slug":"nodes","text":"Nodes"},{"depth":3,"slug":"edges","text":"Edges"},{"depth":3,"slug":"what-ships","text":"What ships"},{"depth":3,"slug":"issues","text":"Issues"},{"depth":2,"slug":"limits","text":"Limits"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/reference/project-graph";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Project graph";
const description = "What the project-graph command returns, field by field, and what counts as a relationship, a shipped file, or an issue." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "reference";
const pageAudience = null;
const pageCategory = ["reference"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Project graph {#project-graph}\n\n`project-graph` describes how a documentation set connects: every map, topic, key and DITAVAL file, and every reference between them, per deliverable. The relationship map page is built from this data, and connected assistants read the same data through the `project_graph` tool. To turn it into a page, see [Mapping a project and writing reports](/dogsbay-xml-docs/finding/reports).\n\n```bash\ndogsbay-xml project-graph .\n```\n\nThe command prints JSON. For the same data as a page, see the [example relationship map](https://dogsbay.ai/dogsbay-xml-docs/examples/relationship-map.html).\n\n## Options {#options}\n\n| Option | What it does |\n| --- | --- |\n| `--map <file>` | Describe one root map, as a deliverable named after the map file without its extension. |\n| `--deliverable <name>` | Describe one deliverable from the project file. |\n| `--no-checks` | Report structure only. Skips grammar validation of the shipped files, the conref element ID audit, and the conref push audit. |\n\nWithout `--map` or `--deliverable`, the graph covers every deliverable in the project file (`project.json`, `project.xml` or `project.yaml`), plus every other DITA file under the project folder, so that files nothing references still appear. When the project defines no deliverables, each root map that no other map references is treated as one.\n\nThe assistant tool takes the same options as `map`, `deliverable` and `checks` (default true).\n\n## The result {#the-result}\n\n```json\n{\n  \"generated\": \"2026-09-14T16:24:01Z\",\n  \"root\": \"/home/me/audacity-demo\",\n  \"scope\": \"all\",\n  \"deliverables\": [\n    { \"name\": \"beginner-mac\", \"map\": \"beginner-guide.ditamap\",\n      \"ditavals\": [\"filters/mac-beginner.ditaval\"],\n      \"ships\": [\"beginner-guide.ditamap\", \"topics/installing-audacity.dita\"] }\n  ],\n  \"nodes\": [\n    { \"id\": \"topics/installing-audacity.dita\", \"kind\": \"topic\", \"type\": \"task\",\n      \"title\": \"Installing Audacity\", \"ships\": [\"full\", \"beginner-mac\"] },\n    { \"id\": \"key:start-here\", \"kind\": \"key\" }\n  ],\n  \"edges\": [\n    { \"from\": \"beginner-guide.ditamap\", \"to\": \"topics/installing-audacity.dita\",\n      \"kind\": \"topicref\", \"line\": 13 },\n    { \"from\": \"key:start-here\", \"to\": \"topics/beginner-start.dita\",\n      \"kind\": \"keytarget\", \"line\": 9, \"via\": \"beginner-guide.ditamap\" }\n  ],\n  \"issues\": [\n    { \"severity\": \"warning\", \"rule\": \"shadowed-key\", \"file\": \"keydefs-glossary.ditamap\",\n      \"line\": 15, \"message\": \"...\" }\n  ]\n}\n```\n\nOptional fields with no value are left out; lists such as `ditavals` can be empty. All paths are relative to the project folder and use forward slashes.\n\n### Deliverables {#deliverables}\n\n| Field | Meaning |\n| --- | --- |\n| `name` | The deliverable name from the project file, or the map file name without its extension for a map. |\n| `map` | Its root map. |\n| `ditavals` | The DITAVAL files the deliverable applies. |\n| `ships` | The IDs of the files the deliverable publishes. See [What ships](#what-ships). |\n\n### Nodes {#nodes}\n\n| Field | Meaning |\n| --- | --- |\n| `id` | The file path, or `key:` followed by the key name. |\n| `kind` | `map`, `topic`, `key` or `ditaval`. |\n| `type` | The root element of a map or topic, such as `bookmap`, `task`, `concept` or `glossentry`. |\n| `title` | The map or topic title. A key reference in a title with no text of its own appears as the key name in brackets, such as `What is [product-name]?`. |\n| `missing` | `true` for a file that something references but that does not exist. |\n| `ships` | The names of the deliverables that publish the file. |\n\nThere is one node per key name, however many maps define the key. Which definition applies depends on the deliverable; see `via` below.\n\n### Edges {#edges}\n\n| Field | Meaning |\n| --- | --- |\n| `from`, `to` | Node IDs. |\n| `kind` | The kind of relationship, below. |\n| `line` | The line of the element that makes the reference. |\n| `via` | For a `keytarget` edge, the map whose key definition made the binding. |\n| `fragment` | For a conref or key reference to an element, the element ID part. |\n| `broken` | `true` when the target does not exist. |\n| `deliverable` | For a `profile` edge, the deliverable that applies the DITAVAL file. |\n\n| Kind | From | To | Made by |\n| --- | --- | --- | --- |\n| `mapref` | map | map | A `mapref`, or a map reference to another map |\n| `topicref` | map | topic | A `topicref`, `chapter` or similar placement in the table of contents |\n| `reltable` | map | topic | A reference in a relationship table cell |\n| `keydef` | map | key | A key definition |\n| `keytarget` | key | file | The file a key definition points to, with `via` naming the map |\n| `keyref` | map or topic | key | A `keyref` attribute |\n| `conkeyref` | topic | key | A `conkeyref` attribute |\n| `conref` | topic | topic | A `conref` attribute |\n| `link` | topic | topic | An `xref` or `link` to a local file |\n| `ditavalref` | map | DITAVAL | A `ditavalref` element |\n| `profile` | map | DITAVAL | A deliverable applying a DITAVAL file to its root map |\n\nA key defined by several maps has one `keytarget` edge for each distinct definition. The deliverable's key space decides which one applies: the first definition wins, as in DITA-OT.\n\nReferences to images and other non-DITA files, and links to external URLs, are not included.\n\n### What ships {#what-ships}\n\nA deliverable ships its root map, the maps it references, the topics placed in their tables of contents, the targets of conrefs from those files, and the files their keys resolve to in that deliverable's key space. A topic that is reached only through a cross-reference or a relationship table cell does not ship.\n\nDITAVAL conditions are not applied to content, so a topic excluded by a filter still ships. Grammar validation covers only files that ship.\n\n### Issues {#issues}\n\n| Rule | Severity | Meaning |\n| --- | --- | --- |\n| `broken-reference` | error | A reference to a file that does not exist. |\n| `undefined-key` | error | A key reference that no deliverable shipping the file can resolve. |\n| `key-resolves-inconsistently` | warning | A key reference that resolves in some of the deliverables shipping the file and not in others. The message names them. |\n| `shadowed-key` | warning | A key defined twice in one key space. The message names the definition that wins. |\n| `unused-key` | info | A key defined in a map that ships, which nothing references. |\n| `orphan-topic` | info | A `.dita` topic that nothing references. |\n| `invalid-dtd` | error, or warning for a validator warning | A file that ships and fails grammar validation. Not reported with `--no-checks`. |\n| `broken-element-id` | error | A reuse reference whose target file exists but lacks the element ID. Not reported with `--no-checks`. |\n| `conref-push` | warning | A conref push with nothing to push into, or a mark with nothing pushed. Not reported with `--no-checks`. |\n\nThe issues describe the structure. For the full set of checks, including metadata and house rules, run [`project-health`](/dogsbay-xml-docs/finding/validation).\n\n## Limits {#limits}\n\n- Each node is a file or a key. Elements are not nodes; a reference to an element carries its ID in `fragment`.\n- The nesting of topic references is not recorded, only the map that places each topic and the line.\n- Key scopes are resolved as far as the editor models them. See [Keys and reuse](/dogsbay-xml-docs/authoring/keys-and-reuse).\n\n## Related {#related}\n\n:::cards\n- **[Mapping a project and writing reports](/dogsbay-xml-docs/finding/reports){icon=\"file-text\"}**\n  Turn the graph into a relationship map or your own page.\n\n- **[The command line](/dogsbay-xml-docs/reference/cli){icon=\"terminal\"}**\n  Every command, including report.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/reference/project-graph.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  pagefindBaseUrl={"/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="project-graph" class="group scroll-mt-20">Project graph<a href="#project-graph" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h1>
      <p><code>project-graph</code> describes how a documentation set connects: every map, topic, key and DITAVAL file, and every reference between them, per deliverable. The relationship map page is built from this data, and connected assistants read the same data through the <code>project_graph</code> tool. To turn it into a page, see <a href="/dogsbay-xml-docs/finding/reports">Mapping a project and writing reports</a>.</p>
      <CodeBlock code={"dogsbay-xml project-graph ."} lang="bash" />
      <p>The command prints JSON. For the same data as a page, see the <a href="https://dogsbay.ai/dogsbay-xml-docs/examples/relationship-map.html">example relationship map</a>.</p>
      <h2 id="options" class="group scroll-mt-20">Options<a href="#options" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Option</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>--map &lt;file&gt;</code></TableCell>
      <TableCell>Describe one root map, as a deliverable named after the map file without its extension.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>--deliverable &lt;name&gt;</code></TableCell>
      <TableCell>Describe one deliverable from the project file.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>--no-checks</code></TableCell>
      <TableCell>Report structure only. Skips grammar validation of the shipped files, the conref element ID audit, and the conref push audit.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Without <code>--map</code> or <code>--deliverable</code>, the graph covers every deliverable in the project file (<code>project.json</code>, <code>project.xml</code> or <code>project.yaml</code>), plus every other DITA file under the project folder, so that files nothing references still appear. When the project defines no deliverables, each root map that no other map references is treated as one.</p>
      <p>The assistant tool takes the same options as <code>map</code>, <code>deliverable</code> and <code>checks</code> (default true).</p>
      <h2 id="the-result" class="group scroll-mt-20">The result<a href="#the-result" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <CodeBlock code={"{\n  \"generated\": \"2026-09-14T16:24:01Z\",\n  \"root\": \"/home/me/audacity-demo\",\n  \"scope\": \"all\",\n  \"deliverables\": [\n    { \"name\": \"beginner-mac\", \"map\": \"beginner-guide.ditamap\",\n      \"ditavals\": [\"filters/mac-beginner.ditaval\"],\n      \"ships\": [\"beginner-guide.ditamap\", \"topics/installing-audacity.dita\"] }\n  ],\n  \"nodes\": [\n    { \"id\": \"topics/installing-audacity.dita\", \"kind\": \"topic\", \"type\": \"task\",\n      \"title\": \"Installing Audacity\", \"ships\": [\"full\", \"beginner-mac\"] },\n    { \"id\": \"key:start-here\", \"kind\": \"key\" }\n  ],\n  \"edges\": [\n    { \"from\": \"beginner-guide.ditamap\", \"to\": \"topics/installing-audacity.dita\",\n      \"kind\": \"topicref\", \"line\": 13 },\n    { \"from\": \"key:start-here\", \"to\": \"topics/beginner-start.dita\",\n      \"kind\": \"keytarget\", \"line\": 9, \"via\": \"beginner-guide.ditamap\" }\n  ],\n  \"issues\": [\n    { \"severity\": \"warning\", \"rule\": \"shadowed-key\", \"file\": \"keydefs-glossary.ditamap\",\n      \"line\": 15, \"message\": \"...\" }\n  ]\n}"} lang="json" />
      <p>Optional fields with no value are left out; lists such as <code>ditavals</code> can be empty. All paths are relative to the project folder and use forward slashes.</p>
      <h3 id="deliverables" class="group scroll-mt-20">Deliverables<a href="#deliverables" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Field</TableHead>
      <TableHead>Meaning</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>name</code></TableCell>
      <TableCell>The deliverable name from the project file, or the map file name without its extension for a map.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>map</code></TableCell>
      <TableCell>Its root map.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ditavals</code></TableCell>
      <TableCell>The DITAVAL files the deliverable applies.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ships</code></TableCell>
      <TableCell>The IDs of the files the deliverable publishes. See <a href="#what-ships">What ships</a>.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h3 id="nodes" class="group scroll-mt-20">Nodes<a href="#nodes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Field</TableHead>
      <TableHead>Meaning</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>id</code></TableCell>
      <TableCell>The file path, or <code>key:</code> followed by the key name.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>kind</code></TableCell>
      <TableCell><code>map</code>, <code>topic</code>, <code>key</code> or <code>ditaval</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>type</code></TableCell>
      <TableCell>The root element of a map or topic, such as <code>bookmap</code>, <code>task</code>, <code>concept</code> or <code>glossentry</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>title</code></TableCell>
      <TableCell>The map or topic title. A key reference in a title with no text of its own appears as the key name in brackets, such as <code>What is [product-name]?</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>missing</code></TableCell>
      <TableCell><code>true</code> for a file that something references but that does not exist.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ships</code></TableCell>
      <TableCell>The names of the deliverables that publish the file.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>There is one node per key name, however many maps define the key. Which definition applies depends on the deliverable; see <code>via</code> below.</p>
      <h3 id="edges" class="group scroll-mt-20">Edges<a href="#edges" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Field</TableHead>
      <TableHead>Meaning</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>from</code>, <code>to</code></TableCell>
      <TableCell>Node IDs.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>kind</code></TableCell>
      <TableCell>The kind of relationship, below.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>line</code></TableCell>
      <TableCell>The line of the element that makes the reference.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>via</code></TableCell>
      <TableCell>For a <code>keytarget</code> edge, the map whose key definition made the binding.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>fragment</code></TableCell>
      <TableCell>For a conref or key reference to an element, the element ID part.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>broken</code></TableCell>
      <TableCell><code>true</code> when the target does not exist.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>deliverable</code></TableCell>
      <TableCell>For a <code>profile</code> edge, the deliverable that applies the DITAVAL file.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Kind</TableHead>
      <TableHead>From</TableHead>
      <TableHead>To</TableHead>
      <TableHead>Made by</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>mapref</code></TableCell>
      <TableCell>map</TableCell>
      <TableCell>map</TableCell>
      <TableCell>A <code>mapref</code>, or a map reference to another map</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>topicref</code></TableCell>
      <TableCell>map</TableCell>
      <TableCell>topic</TableCell>
      <TableCell>A <code>topicref</code>, <code>chapter</code> or similar placement in the table of contents</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>reltable</code></TableCell>
      <TableCell>map</TableCell>
      <TableCell>topic</TableCell>
      <TableCell>A reference in a relationship table cell</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>keydef</code></TableCell>
      <TableCell>map</TableCell>
      <TableCell>key</TableCell>
      <TableCell>A key definition</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>keytarget</code></TableCell>
      <TableCell>key</TableCell>
      <TableCell>file</TableCell>
      <TableCell>The file a key definition points to, with <code>via</code> naming the map</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>keyref</code></TableCell>
      <TableCell>map or topic</TableCell>
      <TableCell>key</TableCell>
      <TableCell>A <code>keyref</code> attribute</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>conkeyref</code></TableCell>
      <TableCell>topic</TableCell>
      <TableCell>key</TableCell>
      <TableCell>A <code>conkeyref</code> attribute</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>conref</code></TableCell>
      <TableCell>topic</TableCell>
      <TableCell>topic</TableCell>
      <TableCell>A <code>conref</code> attribute</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>link</code></TableCell>
      <TableCell>topic</TableCell>
      <TableCell>topic</TableCell>
      <TableCell>An <code>xref</code> or <code>link</code> to a local file</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>ditavalref</code></TableCell>
      <TableCell>map</TableCell>
      <TableCell>DITAVAL</TableCell>
      <TableCell>A <code>ditavalref</code> element</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>profile</code></TableCell>
      <TableCell>map</TableCell>
      <TableCell>DITAVAL</TableCell>
      <TableCell>A deliverable applying a DITAVAL file to its root map</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>A key defined by several maps has one <code>keytarget</code> edge for each distinct definition. The deliverable's key space decides which one applies: the first definition wins, as in DITA-OT.</p>
      <p>References to images and other non-DITA files, and links to external URLs, are not included.</p>
      <h3 id="what-ships" class="group scroll-mt-20">What ships<a href="#what-ships" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <p>A deliverable ships its root map, the maps it references, the topics placed in their tables of contents, the targets of conrefs from those files, and the files their keys resolve to in that deliverable's key space. A topic that is reached only through a cross-reference or a relationship table cell does not ship.</p>
      <p>DITAVAL conditions are not applied to content, so a topic excluded by a filter still ships. Grammar validation covers only files that ship.</p>
      <h3 id="issues" class="group scroll-mt-20">Issues<a href="#issues" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h3>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Rule</TableHead>
      <TableHead>Severity</TableHead>
      <TableHead>Meaning</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>broken-reference</code></TableCell>
      <TableCell>error</TableCell>
      <TableCell>A reference to a file that does not exist.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>undefined-key</code></TableCell>
      <TableCell>error</TableCell>
      <TableCell>A key reference that no deliverable shipping the file can resolve.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>key-resolves-inconsistently</code></TableCell>
      <TableCell>warning</TableCell>
      <TableCell>A key reference that resolves in some of the deliverables shipping the file and not in others. The message names them.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>shadowed-key</code></TableCell>
      <TableCell>warning</TableCell>
      <TableCell>A key defined twice in one key space. The message names the definition that wins.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>unused-key</code></TableCell>
      <TableCell>info</TableCell>
      <TableCell>A key defined in a map that ships, which nothing references.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>orphan-topic</code></TableCell>
      <TableCell>info</TableCell>
      <TableCell>A <code>.dita</code> topic that nothing references.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>invalid-dtd</code></TableCell>
      <TableCell>error, or warning for a validator warning</TableCell>
      <TableCell>A file that ships and fails grammar validation. Not reported with <code>--no-checks</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>broken-element-id</code></TableCell>
      <TableCell>error</TableCell>
      <TableCell>A reuse reference whose target file exists but lacks the element ID. Not reported with <code>--no-checks</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>conref-push</code></TableCell>
      <TableCell>warning</TableCell>
      <TableCell>A conref push with nothing to push into, or a mark with nothing pushed. Not reported with <code>--no-checks</code>.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>The issues describe the structure. For the full set of checks, including metadata and house rules, run <a href="/dogsbay-xml-docs/finding/validation"><code>project-health</code></a>.</p>
      <h2 id="limits" class="group scroll-mt-20">Limits<a href="#limits" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <ul>
      <li>Each node is a file or a key. Elements are not nodes; a reference to an element carries its ID in <code>fragment</code>.</li>
      <li>The nesting of topic references is not recorded, only the map that places each topic and the line.</li>
      <li>Key scopes are resolved as far as the editor models them. See <a href="/dogsbay-xml-docs/authoring/keys-and-reuse">Keys and reuse</a>.</li>
      </ul>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/finding/reports" class="[text-decoration:none] text-foreground">
      <Icon name="file-text" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Mapping a project and writing reports</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Turn the graph into a relationship map or your own page.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/reference/cli" class="[text-decoration:none] text-foreground">
      <Icon name="terminal" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">The command line</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Every command, including report.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

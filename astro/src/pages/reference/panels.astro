---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: reference/panels.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"panel-reference","text":"Panel reference"},{"depth":2,"slug":"left-sidebar","text":"Left sidebar"},{"depth":3,"slug":"explorer","text":"Explorer"},{"depth":3,"slug":"search","text":"Search"},{"depth":3,"slug":"git","text":"Git"},{"depth":3,"slug":"topic-maps","text":"Topic Maps"},{"depth":3,"slug":"xpath-query","text":"XPath Query"},{"depth":3,"slug":"where-used","text":"Where Used"},{"depth":3,"slug":"bookmarks","text":"Bookmarks"},{"depth":2,"slug":"right-sidebar","text":"Right sidebar"},{"depth":3,"slug":"outline","text":"Outline"},{"depth":3,"slug":"ai-agent","text":"AI Agent"},{"depth":3,"slug":"proposals","text":"Proposals"},{"depth":3,"slug":"metadata","text":"Metadata"},{"depth":3,"slug":"properties-navigator-and-helper","text":"Properties, Navigator and Helper"},{"depth":2,"slug":"bottom-panel","text":"Bottom panel"},{"depth":3,"slug":"errors","text":"Errors"},{"depth":3,"slug":"project-validation","text":"Project Validation"},{"depth":3,"slug":"inspector","text":"Inspector"},{"depth":3,"slug":"terminal","text":"Terminal"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/reference/panels";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Panel reference";
const description = "Every panel in the sidebars and the bottom bar, what it shows, and what you can do in it." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "reference";
const pageAudience = null;
const pageCategory = ["reference"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Panel reference {#panel-reference}\n\nThe editor's work happens in panels: two sidebars and a bottom bar. Show and hide them from **View > Appearance**, with Toggle Primary Sidebar, Toggle Bottom Panel and Toggle Secondary Sidebar.\n\nMost panels come from plugins, and each can be turned off individually in **File > Preferences > Plugins**. A panel whose plugin is disabled does not appear at all.\n\n## Left sidebar {#left-sidebar}\n\n### Explorer {#explorer}\n\nThe file tree for the open folder. Always present; it is not a plugin.\n\nOpen, rename, move and delete files, and drag a file onto an agent's chat box to attach it.\n\n### Search {#search}\n\nFind and replace across the project.\n\n| Control | Effect |\n| --- | --- |\n| Match Case | Distinguish upper and lower case. |\n| Match Whole Word | Require whole-word matches. |\n| Use Regular Expression | Treat the term as a regular expression. |\n| Toggle Replace | Show the replacement field. |\n| Preserve Case | Keep the case of what is replaced. |\n| Toggle Search Details | Show the include and exclude filters. |\n\nFilters take comma-separated globs, such as `*.xml,*.java` to include, or `target/**,*.class` to exclude.\n\n### Git {#git}\n\nVersion control for the project. The panel lists changed files and stages them individually or together.\n\nAvailable actions: Stage All, Commit, Commit All, Commit Staged, Amend Last Commit, Discard All Changes, Fetch, Pull, Push, Stash and Pop Stash, plus Create Branch and Checkout Branch.\n\nAuthentication uses your system git configuration, so a repository you can already push to from a terminal works here.\n\n### Topic Maps {#topic-maps}\n\nThe DITA map as a tree: topic references, nested maps and relationship tables. Comes from the DITA plugin.\n\nButtons: Open DITA Map, Refresh Map, Collapse All, and Publish DITA Map, which builds through DITA-OT.\n\n### XPath Query {#xpath-query}\n\nRun an XPath expression across files and list the matches. Takes the same comma-separated glob filters as Search, with Stop and Clear for long runs.\n\n### Where Used {#where-used}\n\nWhat depends on a file or a key, which is the question to ask before renaming anything.\n\n| Control | Effect |\n| --- | --- |\n| Active Document | Find everything that references the document in front. |\n| Key | Inspect one key: its definition and every use. Press Enter to run. |\n| Rebuild | Re-scan the project and rebuild the link index. |\n\nThe index covers references from maps, conrefs, links and images. With a root map configured it also finds references that reach a file indirectly through a key, which a text search cannot.\n\n### Bookmarks {#bookmarks}\n\nPlaces you marked, with Delete Bookmark to remove them. Mark and unmark from **Utilities > Toggle Bookmark**.\n\n## Right sidebar {#right-sidebar}\n\n### Outline {#outline}\n\nThe structure of the document in front, as a tree you can click to navigate. It follows the document type, so a Markdown file outlines by heading and an XML file by element.\n\n### AI Agent {#ai-agent}\n\nThe agent panel, as tabs. The first is the built-in agent; each hosted agent you start gets its own, with its capability tier shown as a badge.\n\nPer-tab actions, from the tab's context menu: Rename session, Export transcript, Copy transcript, and Close session. The add button lists agents this machine can run, with Refresh agent registry and an install action.\n\nSee [the agent](/dogsbay-xml-docs/agent/overview).\n\n### Proposals {#proposals}\n\nChanges an agent has proposed, waiting for your decision: Accept, Reject, Accept all, Reject all, and Resolve for comments. Selecting one moves the editor to the affected part of the document.\n\nSee [reviewing an agent's changes](/dogsbay-xml-docs/agent/proposals).\n\n### Metadata {#metadata}\n\nThe metadata on the current topic, with Refresh, and Normalize to bring it in line with the project's policy. Comes from the DITA plugin. See [metadata](/dogsbay-xml-docs/authoring/metadata).\n\n### Properties, Navigator and Helper {#properties-navigator-and-helper}\n\nDocument properties, navigation aids, and context-sensitive help for the markup at the cursor.\n\n## Bottom panel {#bottom-panel}\n\nThe tabs are Errors, Project Validation, Terminal and Inspector.\n\n### Errors {#errors}\n\nValidation results for the document in front. Selecting an entry jumps to the place it refers to.\n\n### Project Validation {#project-validation}\n\nResults from a project-wide run, such as **Project > Validate > Project**, and output from long-running operations including DITA-OT builds.\n\n### Inspector {#inspector}\n\nTwo tabs. **Link Checker** reports references that do not resolve. **Changes** shows what has changed in the document, with a count in the tab label.\n\n### Terminal {#terminal}\n\nA real terminal in the project folder, with as many tabs as you need. Kill Process stops what is running; the tab's close button ends the session.\n\nThe first terminal opens in the project folder, or in the folder shown in the Explorer when no project is open.\n\n## Related {#related}\n\n:::cards\n- **[Menu reference](./menus){icon=\"menu\"}**\n  Every command in the menu bar.\n\n- **[The command line](./cli){icon=\"terminal\"}**\n  The same operations without the interface.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/reference/panels.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="panel-reference" class="group scroll-mt-20">Panel reference<a href="#panel-reference" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>The editor's work happens in panels: two sidebars and a bottom bar. Show and hide them from <strong>View &gt; Appearance</strong>, with Toggle Primary Sidebar, Toggle Bottom Panel and Toggle Secondary Sidebar.</p>
      <p>Most panels come from plugins, and each can be turned off individually in <strong>File &gt; Preferences &gt; Plugins</strong>. A panel whose plugin is disabled does not appear at all.</p>
      <h2 id="left-sidebar" class="group scroll-mt-20">Left sidebar<a href="#left-sidebar" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <h3 id="explorer" class="group scroll-mt-20">Explorer<a href="#explorer" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The file tree for the open folder. Always present; it is not a plugin.</p>
      <p>Open, rename, move and delete files, and drag a file onto an agent's chat box to attach it.</p>
      <h3 id="search" class="group scroll-mt-20">Search<a href="#search" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Find and replace across the project.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Control</TableHead>
      <TableHead>Effect</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Match Case</TableCell>
      <TableCell>Distinguish upper and lower case.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Match Whole Word</TableCell>
      <TableCell>Require whole-word matches.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Use Regular Expression</TableCell>
      <TableCell>Treat the term as a regular expression.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Toggle Replace</TableCell>
      <TableCell>Show the replacement field.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Preserve Case</TableCell>
      <TableCell>Keep the case of what is replaced.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Toggle Search Details</TableCell>
      <TableCell>Show the include and exclude filters.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Filters take comma-separated globs, such as <code>*.xml,*.java</code> to include, or <code>target/**,*.class</code> to exclude.</p>
      <h3 id="git" class="group scroll-mt-20">Git<a href="#git" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Version control for the project. The panel lists changed files and stages them individually or together.</p>
      <p>Available actions: Stage All, Commit, Commit All, Commit Staged, Amend Last Commit, Discard All Changes, Fetch, Pull, Push, Stash and Pop Stash, plus Create Branch and Checkout Branch.</p>
      <p>Authentication uses your system git configuration, so a repository you can already push to from a terminal works here.</p>
      <h3 id="topic-maps" class="group scroll-mt-20">Topic Maps<a href="#topic-maps" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The DITA map as a tree: topic references, nested maps and relationship tables. Comes from the DITA plugin.</p>
      <p>Buttons: Open DITA Map, Refresh Map, Collapse All, and Publish DITA Map, which builds through DITA-OT.</p>
      <h3 id="xpath-query" class="group scroll-mt-20">XPath Query<a href="#xpath-query" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Run an XPath expression across files and list the matches. Takes the same comma-separated glob filters as Search, with Stop and Clear for long runs.</p>
      <h3 id="where-used" class="group scroll-mt-20">Where Used<a href="#where-used" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>What depends on a file or a key, which is the question to ask before renaming anything.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Control</TableHead>
      <TableHead>Effect</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Active Document</TableCell>
      <TableCell>Find everything that references the document in front.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Key</TableCell>
      <TableCell>Inspect one key: its definition and every use. Press Enter to run.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Rebuild</TableCell>
      <TableCell>Re-scan the project and rebuild the link index.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>The index covers references from maps, conrefs, links and images. With a root map configured it also finds references that reach a file indirectly through a key, which a text search cannot.</p>
      <h3 id="bookmarks" class="group scroll-mt-20">Bookmarks<a href="#bookmarks" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Places you marked, with Delete Bookmark to remove them. Mark and unmark from <strong>Utilities &gt; Toggle Bookmark</strong>.</p>
      <h2 id="right-sidebar" class="group scroll-mt-20">Right sidebar<a href="#right-sidebar" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <h3 id="outline" class="group scroll-mt-20">Outline<a href="#outline" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The structure of the document in front, as a tree you can click to navigate. It follows the document type, so a Markdown file outlines by heading and an XML file by element.</p>
      <h3 id="ai-agent" class="group scroll-mt-20">AI Agent<a href="#ai-agent" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The agent panel, as tabs. The first is the built-in agent; each hosted agent you start gets its own, with its capability tier shown as a badge.</p>
      <p>Per-tab actions, from the tab's context menu: Rename session, Export transcript, Copy transcript, and Close session. The add button lists agents this machine can run, with Refresh agent registry and an install action.</p>
      <p>See <a href="/dogsbay-xml-docs/agent/overview">the agent</a>.</p>
      <h3 id="proposals" class="group scroll-mt-20">Proposals<a href="#proposals" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Changes an agent has proposed, waiting for your decision: Accept, Reject, Accept all, Reject all, and Resolve for comments. Selecting one moves the editor to the affected part of the document.</p>
      <p>See <a href="/dogsbay-xml-docs/agent/proposals">reviewing an agent's changes</a>.</p>
      <h3 id="metadata" class="group scroll-mt-20">Metadata<a href="#metadata" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>The metadata on the current topic, with Refresh, and Normalize to bring it in line with the project's policy. Comes from the DITA plugin. See <a href="/dogsbay-xml-docs/authoring/metadata">metadata</a>.</p>
      <h3 id="properties-navigator-and-helper" class="group scroll-mt-20">Properties, Navigator and Helper<a href="#properties-navigator-and-helper" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Document properties, navigation aids, and context-sensitive help for the markup at the cursor.</p>
      <h2 id="bottom-panel" class="group scroll-mt-20">Bottom panel<a href="#bottom-panel" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The tabs are Errors, Project Validation, Terminal and Inspector.</p>
      <h3 id="errors" class="group scroll-mt-20">Errors<a href="#errors" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Validation results for the document in front. Selecting an entry jumps to the place it refers to.</p>
      <h3 id="project-validation" class="group scroll-mt-20">Project Validation<a href="#project-validation" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Results from a project-wide run, such as <strong>Project &gt; Validate &gt; Project</strong>, and output from long-running operations including DITA-OT builds.</p>
      <h3 id="inspector" class="group scroll-mt-20">Inspector<a href="#inspector" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>Two tabs. <strong>Link Checker</strong> reports references that do not resolve. <strong>Changes</strong> shows what has changed in the document, with a count in the tab label.</p>
      <h3 id="terminal" class="group scroll-mt-20">Terminal<a href="#terminal" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h3>
      <p>A real terminal in the project folder, with as many tabs as you need. Kill Process stops what is running; the tab's close button ends the session.</p>
      <p>The first terminal opens in the project folder, or in the folder shown in the Explorer when no project is open.</p>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./menus" class="[text-decoration:none] text-foreground">
      <Icon name="menu" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Menu reference</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Every command in the menu bar.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./cli" class="[text-decoration:none] text-foreground">
      <Icon name="terminal" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">The command line</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">The same operations without the interface.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: reference/cli.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"the-command-line","text":"The command line"},{"depth":2,"slug":"reading-a-document","text":"Reading a document"},{"depth":2,"slug":"asking-about-the-project","text":"Asking about the project"},{"depth":2,"slug":"validating-a-set","text":"Validating a set"},{"depth":2,"slug":"metadata","text":"Metadata"},{"depth":2,"slug":"surveying-dita-features","text":"Surveying DITA features"},{"depth":2,"slug":"changing-files","text":"Changing files"},{"depth":2,"slug":"publishing","text":"Publishing"},{"depth":2,"slug":"driving-a-running-editor","text":"Driving a running editor"},{"depth":2,"slug":"agents-and-review","text":"Agents and review"},{"depth":2,"slug":"projects","text":"Projects"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/reference/cli";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "The command line";
const description = "Find the dogsbay-xml command for the information or change that you need." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "reference";
const pageAudience = null;
const pageCategory = ["reference"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# The command line {#the-command-line}\n\n`dogsbay-xml` runs the same operations as the editor's menus. The editor installation includes the command. See [The command line](/dogsbay-xml-docs/getting-started/install#the-command-line) to find it or to run it on a machine where you cannot use an installer.\n\nMost commands work on files and need nothing running. A few drive an editor that is already open, and those need the integration server.\n\nRun `dogsbay-xml --help` for the full list, and `dogsbay-xml <command> --help` for one command's options.\n\n> [!IMPORTANT]\n> The refactoring commands print a plan and change nothing until you add `--apply`. The structural editors, `edit-map` and `edit-reltable`, work the other way round: they write unless you pass `--dry-run`.\n\n## Reading a document {#reading-a-document}\n\n| Command | What it does |\n| --- | --- |\n| `validate` | Validate against XSD, DTD, or RELAX NG, resolving catalogs. DITA files fall back to the bundled DITA grammars. |\n| `parse` | Print the root element, namespace, and encoding. |\n| `info` | Report encoding, grammar, root element and size. |\n| `query` | Run an XPath expression over a file or a glob. |\n| `transform` | Apply an XSLT stylesheet. |\n| `format` | Pretty-print using the project's house style. |\n| `reflow` | Reflow prose to one sentence per line, leaving verbatim blocks alone. |\n| `preview` | Render the styled DITA preview to HTML. |\n\n## Asking about the project {#asking-about-the-project}\n\nThese commands answer questions about more than one file.\n\n| Command | What it answers |\n| --- | --- |\n| `where-used` | What references this file? |\n| `keys` | What keys does this root map define, and what do they resolve to? |\n| `check-links` | Which references point at something missing? |\n| `conref-audit` | Which reuse references name an element ID that no longer exists? |\n| `health` | Broken references, undefined and unused keys, and orphaned topics. |\n| `project-health` | Report broken references, key problems, orphaned topics, grammar validation, metadata policy violations, and open proposals. With `--schematron`, also report the project's house rules. |\n\n`check-links`, `health`, and `project-health` exit with a nonzero status when they find a problem, so they work as pipeline gates.\n\n```bash\ndogsbay-xml project-health . --schematron house-style.sch\n```\n\n`project-health` prints every finding and then a summary of the counts, with the house rules and the metadata policy broken down by rule. Add `--summary` for the counts alone. See [validating a project](/dogsbay-xml-docs/finding/validation#reading-the-result).\n\n## Validating a set {#validating-a-set}\n\n| Command | What it does |\n| --- | --- |\n| `validate-project` | Validate every file in a scope: a map's publication set, a glob, or a folder. |\n| `validate-deliverables` | Validate each deliverable, with its own map and conditions. |\n| `validate-ot` | Run DITA-OT preprocessing per deliverable, which catches key and conref resolution failures that static validation cannot. Requires DITA-OT. |\n| `schematron` | Apply a Schematron schema to one document. |\n| `schematron-project` | Apply one across a scope. |\n| `validate-conditions` | Report profiling values that the subject scheme does not allow. |\n\n## Metadata {#metadata}\n\n| Command | What it does |\n| --- | --- |\n| `metadata-audit` | Report where required metadata is missing or wrong. |\n| `metadata-set` | Set, fill, append, or remove fields in bulk, preserving the rest of the prolog. |\n| `metadata-export-schematron` | Compile the metadata policy to ISO Schematron, so the same rules run anywhere. |\n\n## Surveying DITA features {#surveying-dita-features}\n\nEach of these reports on one DITA mechanism across the project: `reltable-audit`, `list-branches`, `list-subjects`, `keyword-audit`, `index-audit`, `glossary-audit`, `conref-push-audit`, `chunk-audit`, and `specialization-info`.\n\n## Changing files {#changing-files}\n\nReference-safe edits. All are dry-run first.\n\n| Command | What it does |\n| --- | --- |\n| `rename-file` | Rename a file and every reference to it. |\n| `rename-key` | Rename a key and every use of it. |\n| `rename-element-id` | Rename an element ID, updating conrefs that point at it. |\n| `rename-profile-value` | Rename a profiling value, such as a platform or audience. |\n| `delete-file` | Delete safely, reporting inbound references first. |\n| `retarget` | Point every reference from one file at another. |\n| `keyify` | Convert direct references to key references, adding the key definitions. |\n| `inline-key` | The reverse: turn a key reference back into a direct one. |\n| `extract-conref` | Move an element into a reuse topic and leave a conref behind. |\n| `inline-conref` | Replace a conref with a copy of the content it pulls in. |\n| `create-keydef` | Add a text key definition. |\n| `merge-keydefs` | Remove duplicate key definitions that are shadowed anyway. |\n| `split-topic` | Split a topic at its sections, adding the new topics to the map. |\n\nStructural editing of maps and relationship tables preserves formatting and keeps references intact: `edit-map` and `edit-reltable`. Both take an operation as their second argument, and both write unless you pass `--dry-run`.\n\n## Publishing {#publishing}\n\n| Command | What it does |\n| --- | --- |\n| `build` | Build one deliverable or all of them with DITA-OT, using the transform type, conditions, and parameters that the project defines. |\n\n## Driving a running editor {#driving-a-running-editor}\n\nThese need the integration server, which you turn on in **File > Settings > Server**.\n\n| Command | What it does |\n| --- | --- |\n| `open`, `close`, `list`, `save` | Manage open documents. |\n| `selection`, `goto-line`, `cursor`, `select-element`, `wrap` | Move around and edit at the cursor. |\n| `author` | Drive the Author view: `outline`, `switch`, `insert`, `set-text`, `issues`. |\n| `screenshot` | Capture the editor window. |\n| `status` | Report whether the editor is reachable, and print MCP configuration for AI assistants. |\n\n## Agents and review {#agents-and-review}\n\n| Command | What it does |\n| --- | --- |\n| `review` | List, accept, reject, or comment on agent proposals in a document. |\n| `sessions` | List the agent sessions connected to the editor. |\n| `agents` | List Agent Client Protocol agents and whether this machine can run them. |\n| `audit-log` | Show what agents changed in this project, newest first. |\n\n## Projects {#projects}\n\n`project create`, `project open`, `project list`, and `project info` manage projects from the command line.\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/reference/cli.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  pagefindBaseUrl={"/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="the-command-line" class="group scroll-mt-20">The command line<a href="#the-command-line" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h1>
      <p><code>dogsbay-xml</code> runs the same operations as the editor's menus. The editor installation includes the command. See <a href="/dogsbay-xml-docs/getting-started/install#the-command-line">The command line</a> to find it or to run it on a machine where you cannot use an installer.</p>
      <p>Most commands work on files and need nothing running. A few drive an editor that is already open, and those need the integration server.</p>
      <p>Run <code>dogsbay-xml --help</code> for the full list, and <code>dogsbay-xml &lt;command&gt; --help</code> for one command's options.</p>
      <Alert variant="info" class="my-4">
        <AlertTitle>Important</AlertTitle>
        <AlertDescription>
          <p>The refactoring commands print a plan and change nothing until you add <code>--apply</code>. The structural editors, <code>edit-map</code> and <code>edit-reltable</code>, work the other way round: they write unless you pass <code>--dry-run</code>.</p>
        </AlertDescription>
      </Alert>
      <h2 id="reading-a-document" class="group scroll-mt-20">Reading a document<a href="#reading-a-document" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>validate</code></TableCell>
      <TableCell>Validate against XSD, DTD, or RELAX NG, resolving catalogs. DITA files fall back to the bundled DITA grammars.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>parse</code></TableCell>
      <TableCell>Print the root element, namespace, and encoding.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>info</code></TableCell>
      <TableCell>Report encoding, grammar, root element and size.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>query</code></TableCell>
      <TableCell>Run an XPath expression over a file or a glob.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>transform</code></TableCell>
      <TableCell>Apply an XSLT stylesheet.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>format</code></TableCell>
      <TableCell>Pretty-print using the project's house style.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>reflow</code></TableCell>
      <TableCell>Reflow prose to one sentence per line, leaving verbatim blocks alone.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>preview</code></TableCell>
      <TableCell>Render the styled DITA preview to HTML.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="asking-about-the-project" class="group scroll-mt-20">Asking about the project<a href="#asking-about-the-project" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>These commands answer questions about more than one file.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>What it answers</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>where-used</code></TableCell>
      <TableCell>What references this file?</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>keys</code></TableCell>
      <TableCell>What keys does this root map define, and what do they resolve to?</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>check-links</code></TableCell>
      <TableCell>Which references point at something missing?</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>conref-audit</code></TableCell>
      <TableCell>Which reuse references name an element ID that no longer exists?</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>health</code></TableCell>
      <TableCell>Broken references, undefined and unused keys, and orphaned topics.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>project-health</code></TableCell>
      <TableCell>Report broken references, key problems, orphaned topics, grammar validation, metadata policy violations, and open proposals. With <code>--schematron</code>, also report the project's house rules.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p><code>check-links</code>, <code>health</code>, and <code>project-health</code> exit with a nonzero status when they find a problem, so they work as pipeline gates.</p>
      <CodeBlock code={"dogsbay-xml project-health . --schematron house-style.sch"} lang="bash" />
      <p><code>project-health</code> prints every finding and then a summary of the counts, with the house rules and the metadata policy broken down by rule. Add <code>--summary</code> for the counts alone. See <a href="/dogsbay-xml-docs/finding/validation#reading-the-result">validating a project</a>.</p>
      <h2 id="validating-a-set" class="group scroll-mt-20">Validating a set<a href="#validating-a-set" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>validate-project</code></TableCell>
      <TableCell>Validate every file in a scope: a map's publication set, a glob, or a folder.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>validate-deliverables</code></TableCell>
      <TableCell>Validate each deliverable, with its own map and conditions.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>validate-ot</code></TableCell>
      <TableCell>Run DITA-OT preprocessing per deliverable, which catches key and conref resolution failures that static validation cannot. Requires DITA-OT.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>schematron</code></TableCell>
      <TableCell>Apply a Schematron schema to one document.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>schematron-project</code></TableCell>
      <TableCell>Apply one across a scope.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>validate-conditions</code></TableCell>
      <TableCell>Report profiling values that the subject scheme does not allow.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="metadata" class="group scroll-mt-20">Metadata<a href="#metadata" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>metadata-audit</code></TableCell>
      <TableCell>Report where required metadata is missing or wrong.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>metadata-set</code></TableCell>
      <TableCell>Set, fill, append, or remove fields in bulk, preserving the rest of the prolog.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>metadata-export-schematron</code></TableCell>
      <TableCell>Compile the metadata policy to ISO Schematron, so the same rules run anywhere.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="surveying-dita-features" class="group scroll-mt-20">Surveying DITA features<a href="#surveying-dita-features" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>Each of these reports on one DITA mechanism across the project: <code>reltable-audit</code>, <code>list-branches</code>, <code>list-subjects</code>, <code>keyword-audit</code>, <code>index-audit</code>, <code>glossary-audit</code>, <code>conref-push-audit</code>, <code>chunk-audit</code>, and <code>specialization-info</code>.</p>
      <h2 id="changing-files" class="group scroll-mt-20">Changing files<a href="#changing-files" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>Reference-safe edits. All are dry-run first.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>rename-file</code></TableCell>
      <TableCell>Rename a file and every reference to it.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>rename-key</code></TableCell>
      <TableCell>Rename a key and every use of it.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>rename-element-id</code></TableCell>
      <TableCell>Rename an element ID, updating conrefs that point at it.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>rename-profile-value</code></TableCell>
      <TableCell>Rename a profiling value, such as a platform or audience.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>delete-file</code></TableCell>
      <TableCell>Delete safely, reporting inbound references first.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>retarget</code></TableCell>
      <TableCell>Point every reference from one file at another.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>keyify</code></TableCell>
      <TableCell>Convert direct references to key references, adding the key definitions.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>inline-key</code></TableCell>
      <TableCell>The reverse: turn a key reference back into a direct one.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>extract-conref</code></TableCell>
      <TableCell>Move an element into a reuse topic and leave a conref behind.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>inline-conref</code></TableCell>
      <TableCell>Replace a conref with a copy of the content it pulls in.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>create-keydef</code></TableCell>
      <TableCell>Add a text key definition.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>merge-keydefs</code></TableCell>
      <TableCell>Remove duplicate key definitions that are shadowed anyway.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>split-topic</code></TableCell>
      <TableCell>Split a topic at its sections, adding the new topics to the map.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Structural editing of maps and relationship tables preserves formatting and keeps references intact: <code>edit-map</code> and <code>edit-reltable</code>. Both take an operation as their second argument, and both write unless you pass <code>--dry-run</code>.</p>
      <h2 id="publishing" class="group scroll-mt-20">Publishing<a href="#publishing" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>build</code></TableCell>
      <TableCell>Build one deliverable or all of them with DITA-OT, using the transform type, conditions, and parameters that the project defines.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="driving-a-running-editor" class="group scroll-mt-20">Driving a running editor<a href="#driving-a-running-editor" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p>These need the integration server, which you turn on in <strong>File &gt; Settings &gt; Server</strong>.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>open</code>, <code>close</code>, <code>list</code>, <code>save</code></TableCell>
      <TableCell>Manage open documents.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>selection</code>, <code>goto-line</code>, <code>cursor</code>, <code>select-element</code>, <code>wrap</code></TableCell>
      <TableCell>Move around and edit at the cursor.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>author</code></TableCell>
      <TableCell>Drive the Author view: <code>outline</code>, <code>switch</code>, <code>insert</code>, <code>set-text</code>, <code>issues</code>.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>screenshot</code></TableCell>
      <TableCell>Capture the editor window.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>status</code></TableCell>
      <TableCell>Report whether the editor is reachable, and print MCP configuration for AI assistants.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="agents-and-review" class="group scroll-mt-20">Agents and review<a href="#agents-and-review" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Command</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>review</code></TableCell>
      <TableCell>List, accept, reject, or comment on agent proposals in a document.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>sessions</code></TableCell>
      <TableCell>List the agent sessions connected to the editor.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>agents</code></TableCell>
      <TableCell>List Agent Client Protocol agents and whether this machine can run them.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>audit-log</code></TableCell>
      <TableCell>Show what agents changed in this project, newest first.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="projects" class="group scroll-mt-20">Projects<a href="#projects" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <p><code>project create</code>, <code>project open</code>, <code>project list</code>, and <code>project info</code> manage projects from the command line.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: reference/settings.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"settings-reference","text":"Settings reference"},{"depth":2,"slug":"server","text":"Server"},{"depth":2,"slug":"plugins","text":"Plugins"},{"depth":2,"slug":"bindings","text":"Bindings"},{"depth":2,"slug":"text","text":"Text"},{"depth":2,"slug":"views","text":"Views"},{"depth":2,"slug":"xml","text":"XML"},{"depth":2,"slug":"format","text":"Format"},{"depth":2,"slug":"system","text":"System"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/reference/settings";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Settings reference";
const description = "Every page in File > Settings, and what each one controls." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "reference";
const pageAudience = null;
const pageCategory = ["reference"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Settings reference {#settings-reference}\n\n**File > Settings** opens the editor's settings. The pages are listed down the left; a change applies when you select **OK**, and **Cancel** discards everything you changed since the dialog opened.\n\nSettings are stored in `~/.dogsbay/settings.xml` and apply to every project. Settings that belong to a project, such as its type, root map, framework and formatting house style, live in that project's own `.dogsbay/config.xml`, which you can commit and share. See [Format](#format) below for how to write the current formatting into it.\n\n## Server {#server}\n\nThe integration server, and the defaults for agents.\n\n**Enable integration server (MCP / CLI / REST)** lets external tools drive the editor over localhost: the MCP endpoint for AI assistants, the `dogsbay-xml` command line's editor commands, and the REST API. It is off until you turn it on. The in-app agent does not need it; a [hosted agent](/dogsbay-xml-docs/agent/hosted-agents) does. When the server is running, the page shows the address it is on.\n\n| Setting | What it does |\n| --- | --- |\n| Port (0 = automatic) | The port to bind. Zero picks a free one. |\n| Expose MCP endpoint (/mcp) | Serves the editor's tools to AI assistants. |\n| Expose JSON-RPC endpoint (/rpc) | Serves the command line's editor commands. |\n| Copy MCP install command | Copies a ready-to-paste `claude mcp add` command, with the access token, to the clipboard. |\n| Regenerate token | Issues a new access token. Existing MCP and CLI clients must be reconfigured with it. |\n\n**Hosted agents** sets what a new hosted agent session starts with. **Default tier for a new agent** is the preselected answer: T1 (commands only), T2 (read files) or T3 (read and write files). You are still asked each time a session starts. **Agent edits to DITA documents land as proposals for review** keeps an agent's changes as tracked [proposals](/dogsbay-xml-docs/agent/proposals) rather than direct edits.\n\n**Agent sessions** controls how long the built-in agent's transcripts are kept in `~/.xagent/sessions`: Forever, or 90, 60, 30, 14 or 7 days. Older ones are deleted when the editor starts.\n\n## Plugins {#plugins}\n\nEvery plugin the editor has loaded, with its license, and a switch for each. Most panels come from a plugin, and turning one off removes its panel entirely. **Open Plugins Folder** opens the directory plugins are installed into.\n\n## Bindings {#bindings}\n\nEvery command in the editor and the key it answers to. See the [keyboard shortcut reference](/dogsbay-xml-docs/reference/shortcuts) for the defaults.\n\nFilter the list by typing in the box. With a command selected:\n\n| Button | What it does |\n| --- | --- |\n| Change… | Asks for a key. Press the combination you want; Escape on its own cancels. |\n| Bind nothing | Leaves the command with no key. |\n| Reset | Puts the command back to its default. |\n| Reset all | Puts every command back to its default. |\n\nOnly the commands you change are stored, so a default that improves in a later release reaches you. A key already taken by another command is reported when you choose it.\n\n## Text {#text}\n\nHow the editor draws text.\n\nChoose the **font** and **size** for the editor. Fonts marked ★ are bundled with the editor and look the same on every machine.\n\nThe list below sets the **color and style of each kind of markup**: element names, attribute names and values, comments, CDATA, entity references, DTD declarations, DITA and AsciiDoc constructs, and the rest. Select a kind to change its color and whether it is bold or italic.\n\n**Antialiase Text** smooths glyph edges. **Convert to Spaces** inserts spaces when you press Tab rather than a tab character.\n\n## Views {#views}\n\nHow documents open and behave.\n\n| Group | Setting |\n| --- | --- |\n| General | Synchronise Selection between Views; Open multiple occurrences of the same Document; Scroll Document Tabs; Reopen files from previous session |\n| XML Types | Check for Type opening Document; Prompt to create a Type when no Type found; Validate Document on opening; Set Schema or DTD defined in Document |\n| Scenario Execution | Hide Scenario Execution Dialog when complete |\n| XInclude | Open Resolve XInclude in New Document |\n| XPath Editor | Generate Unique XPath |\n| Project | Show full Path for Documents |\n\n## XML {#xml}\n\nParsing and catalogs.\n\nThree groups:\n\n**Non Validating Feature** holds **Load DTD Grammar**, which loads the DTD even when the document is not being validated, so content completion knows the vocabulary.\n\n**Catalogs** lists the OASIS catalog files used to resolve public identifiers and URIs. The DITA catalogs ship with the editor and are already here; **Add …** registers your own and **Delete** removes the selected one. **Prefer Public Identifiers** resolves by public identifier before system identifier.\n\n**Prefix Namespace Mappings** is a table of the prefixes the editor uses for known namespaces, so generated markup and XPath expressions use the prefix you expect.\n\n## Format {#format}\n\nThe house style used by **Format Document**, by format-on-save, and by the `dogsbay-xml format` command, so every route produces the same file.\n\n| Setting | What it does |\n| --- | --- |\n| Indent | Spaces or tabs, and how many. |\n| Max line width | Where to wrap. Zero never hard-wraps prose. |\n| Newline | Platform, LF or CRLF. |\n| Final newline | Ends the file with a newline. |\n| Preserve mixed content | Keeps inline markup inline rather than breaking it onto its own lines. |\n| Preserve blank lines | Keeps one blank line and collapses the extras. |\n| Semantic line breaks | Keeps one sentence per line. |\n| Sentence continuation indent | How a wrapped sentence is indented. |\n| Format on save | Reformats a document whenever you save it. |\n\n**Save to project (.dogsbay/config.xml)…** writes the current style into the open project so everyone working on it formats identically.\n\n## System {#system}\n\nThe machine, rather than the editing.\n\n| Group | What it controls |\n| --- | --- |\n| Look And Feel | The editor's theme. |\n| Browser | Which browser opens external links. Linux and other Unix systems only; macOS and Windows use the system default and the group is not shown. |\n| Proxy Configuration | Host address and port for a proxy, and whether to use one. |\n| Extensions | Extra jars and directories added to the editor's classpath. **Add Jar …**, **Add Dir …** and **Delete**. |\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/reference/settings.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="settings-reference" class="group scroll-mt-20">Settings reference<a href="#settings-reference" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p><strong>File &gt; Settings</strong> opens the editor's settings. The pages are listed down the left; a change applies when you select <strong>OK</strong>, and <strong>Cancel</strong> discards everything you changed since the dialog opened.</p>
      <p>Settings are stored in <code>~/.dogsbay/settings.xml</code> and apply to every project. Settings that belong to a project, such as its type, root map, framework and formatting house style, live in that project's own <code>.dogsbay/config.xml</code>, which you can commit and share. See <a href="#format">Format</a> below for how to write the current formatting into it.</p>
      <h2 id="server" class="group scroll-mt-20">Server<a href="#server" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The integration server, and the defaults for agents.</p>
      <p><strong>Enable integration server (MCP / CLI / REST)</strong> lets external tools drive the editor over localhost: the MCP endpoint for AI assistants, the <code>dogsbay-xml</code> command line's editor commands, and the REST API. It is off until you turn it on. The in-app agent does not need it; a <a href="/dogsbay-xml-docs/agent/hosted-agents">hosted agent</a> does. When the server is running, the page shows the address it is on.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Setting</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Port (0 = automatic)</TableCell>
      <TableCell>The port to bind. Zero picks a free one.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Expose MCP endpoint (/mcp)</TableCell>
      <TableCell>Serves the editor's tools to AI assistants.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Expose JSON-RPC endpoint (/rpc)</TableCell>
      <TableCell>Serves the command line's editor commands.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Copy MCP install command</TableCell>
      <TableCell>Copies a ready-to-paste <code>claude mcp add</code> command, with the access token, to the clipboard.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Regenerate token</TableCell>
      <TableCell>Issues a new access token. Existing MCP and CLI clients must be reconfigured with it.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p><strong>Hosted agents</strong> sets what a new hosted agent session starts with. <strong>Default tier for a new agent</strong> is the preselected answer: T1 (commands only), T2 (read files) or T3 (read and write files). You are still asked each time a session starts. <strong>Agent edits to DITA documents land as proposals for review</strong> keeps an agent's changes as tracked <a href="/dogsbay-xml-docs/agent/proposals">proposals</a> rather than direct edits.</p>
      <p><strong>Agent sessions</strong> controls how long the built-in agent's transcripts are kept in <code>~/.xagent/sessions</code>: Forever, or 90, 60, 30, 14 or 7 days. Older ones are deleted when the editor starts.</p>
      <h2 id="plugins" class="group scroll-mt-20">Plugins<a href="#plugins" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Every plugin the editor has loaded, with its license, and a switch for each. Most panels come from a plugin, and turning one off removes its panel entirely. <strong>Open Plugins Folder</strong> opens the directory plugins are installed into.</p>
      <h2 id="bindings" class="group scroll-mt-20">Bindings<a href="#bindings" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Every command in the editor and the key it answers to. See the <a href="/dogsbay-xml-docs/reference/shortcuts">keyboard shortcut reference</a> for the defaults.</p>
      <p>Filter the list by typing in the box. With a command selected:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Button</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Change…</TableCell>
      <TableCell>Asks for a key. Press the combination you want; Escape on its own cancels.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Bind nothing</TableCell>
      <TableCell>Leaves the command with no key.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Reset</TableCell>
      <TableCell>Puts the command back to its default.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Reset all</TableCell>
      <TableCell>Puts every command back to its default.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p>Only the commands you change are stored, so a default that improves in a later release reaches you. A key already taken by another command is reported when you choose it.</p>
      <h2 id="text" class="group scroll-mt-20">Text<a href="#text" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>How the editor draws text.</p>
      <p>Choose the <strong>font</strong> and <strong>size</strong> for the editor. Fonts marked ★ are bundled with the editor and look the same on every machine.</p>
      <p>The list below sets the <strong>color and style of each kind of markup</strong>: element names, attribute names and values, comments, CDATA, entity references, DTD declarations, DITA and AsciiDoc constructs, and the rest. Select a kind to change its color and whether it is bold or italic.</p>
      <p><strong>Antialiase Text</strong> smooths glyph edges. <strong>Convert to Spaces</strong> inserts spaces when you press Tab rather than a tab character.</p>
      <h2 id="views" class="group scroll-mt-20">Views<a href="#views" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>How documents open and behave.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Group</TableHead>
      <TableHead>Setting</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>General</TableCell>
      <TableCell>Synchronise Selection between Views; Open multiple occurrences of the same Document; Scroll Document Tabs; Reopen files from previous session</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>XML Types</TableCell>
      <TableCell>Check for Type opening Document; Prompt to create a Type when no Type found; Validate Document on opening; Set Schema or DTD defined in Document</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Scenario Execution</TableCell>
      <TableCell>Hide Scenario Execution Dialog when complete</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>XInclude</TableCell>
      <TableCell>Open Resolve XInclude in New Document</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>XPath Editor</TableCell>
      <TableCell>Generate Unique XPath</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Project</TableCell>
      <TableCell>Show full Path for Documents</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="xml" class="group scroll-mt-20">XML<a href="#xml" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Parsing and catalogs.</p>
      <p>Three groups:</p>
      <p><strong>Non Validating Feature</strong> holds <strong>Load DTD Grammar</strong>, which loads the DTD even when the document is not being validated, so content completion knows the vocabulary.</p>
      <p><strong>Catalogs</strong> lists the OASIS catalog files used to resolve public identifiers and URIs. The DITA catalogs ship with the editor and are already here; <strong>Add …</strong> registers your own and <strong>Delete</strong> removes the selected one. <strong>Prefer Public Identifiers</strong> resolves by public identifier before system identifier.</p>
      <p><strong>Prefix Namespace Mappings</strong> is a table of the prefixes the editor uses for known namespaces, so generated markup and XPath expressions use the prefix you expect.</p>
      <h2 id="format" class="group scroll-mt-20">Format<a href="#format" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The house style used by <strong>Format Document</strong>, by format-on-save, and by the <code>dogsbay-xml format</code> command, so every route produces the same file.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Setting</TableHead>
      <TableHead>What it does</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Indent</TableCell>
      <TableCell>Spaces or tabs, and how many.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Max line width</TableCell>
      <TableCell>Where to wrap. Zero never hard-wraps prose.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Newline</TableCell>
      <TableCell>Platform, LF or CRLF.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Final newline</TableCell>
      <TableCell>Ends the file with a newline.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Preserve mixed content</TableCell>
      <TableCell>Keeps inline markup inline rather than breaking it onto its own lines.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Preserve blank lines</TableCell>
      <TableCell>Keeps one blank line and collapses the extras.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Semantic line breaks</TableCell>
      <TableCell>Keeps one sentence per line.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Sentence continuation indent</TableCell>
      <TableCell>How a wrapped sentence is indented.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Format on save</TableCell>
      <TableCell>Reformats a document whenever you save it.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <p><strong>Save to project (.dogsbay/config.xml)…</strong> writes the current style into the open project so everyone working on it formats identically.</p>
      <h2 id="system" class="group scroll-mt-20">System<a href="#system" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The machine, rather than the editing.</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Group</TableHead>
      <TableHead>What it controls</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell>Look And Feel</TableCell>
      <TableCell>The editor's theme.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Browser</TableCell>
      <TableCell>Which browser opens external links. Linux and other Unix systems only; macOS and Windows use the system default and the group is not shown.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Proxy Configuration</TableCell>
      <TableCell>Host address and port for a proxy, and whether to use one.</TableCell>
      </TableRow>
      <TableRow>
      <TableCell>Extensions</TableCell>
      <TableCell>Extra jars and directories added to the editor's classpath. <strong>Add Jar …</strong>, <strong>Add Dir …</strong> and <strong>Delete</strong>.</TableCell>
      </TableRow>
      </TableBody>
      </Table>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

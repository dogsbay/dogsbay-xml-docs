---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: start-here/tutorial.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";
import Step from "@ui/steps/Step.astro";
import Steps from "@ui/steps/Steps.astro";

const headings = [{"depth":1,"slug":"tutorial-fixing-a-dita-guide","text":"Tutorial: fixing a DITA guide"},{"depth":2,"slug":"step-1-open-the-sample-project","text":"Step 1: Open the sample project"},{"depth":2,"slug":"step-2-see-what-is-wrong","text":"Step 2: See what is wrong"},{"depth":2,"slug":"step-3-fix-one-problem-by-hand","text":"Step 3: Fix one problem by hand"},{"depth":2,"slug":"step-4-let-the-agent-do-the-rest","text":"Step 4: Let the agent do the rest"},{"depth":2,"slug":"step-5-review-what-it-proposes","text":"Step 5: Review what it proposes"},{"depth":2,"slug":"step-6-check-the-project-again","text":"Step 6: Check the project again"},{"depth":2,"slug":"what-you-learned","text":"What you learned"},{"depth":2,"slug":"where-to-go-next","text":"Where to go next"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/start-here/tutorial";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Tutorial: fixing a DITA guide";
const description = "Open a real DITA project that has problems in it, find them with the project tools, and fix some of them with the AI agent." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "tutorial";
const pageAudience = null;
const pageCategory = ["start-here"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Tutorial: fixing a DITA guide {#tutorial-fixing-a-dita-guide}\n\nIn this tutorial you inherit a documentation set that does not build cleanly, find out what is wrong with it, and fix part of it with the AI agent.\n\nThe project is a DITA user guide for the Audacity audio editor. It ships with the editor, and it is deliberately broken: topics without descriptions, links that point nowhere, a hardcoded product name that should be a key, and conditions that no subject scheme allows.\n\n**Time:** about 30 minutes. **You need:** DogsBay XML installed. See [Installing](./install). An API key for the agent section is useful but not required until then.\n\n## Step 1: Open the sample project {#step-1-open-the-sample-project}\n\n:::steps\n1. **Select File > Open Sample Project** The editor asks where to put it.\n2. **Choose a location** The sample is copied into a new `audacity-demo` folder inside the location you choose. It never uses the folder you chose as the project itself, and it never overwrites anything: if `audacity-demo` exists, the copy becomes `audacity-demo-2`.\n3. **Wait for the project to open** The Map Explorer loads `audacity-guide.ditamap`, and publishing is configured, without you setting anything up.\n:::\n\nYou now have your own copy to break further and repair.\n\n## Step 2: See what is wrong {#step-2-see-what-is-wrong}\n\nRather than opening files to look for problems, ask the project.\n\nRun the health check from the command line:\n\n```bash\nbin/dogsbay-xml project-health .\n```\n\nThe report covers three kinds of problem at once: references that point at something missing, keys that are used but never defined or defined but never used, topics that no map includes, and files that fail validation against their grammar.\n\nRead the output before you change anything. The point of this step is that the answer is a property of the project, not of the file you happen to have open.\n\n> [!TIP]\n> `check-links` and `health` are narrower and faster if you only want one part of that picture.\n\n## Step 3: Fix one problem by hand {#step-3-fix-one-problem-by-hand}\n\nPick a topic that has no short description.\n\n:::steps\n1. **Open the topic** Select it in the Explorer.\n2. **Switch to the Author view** Select **View > Document Views > Author**, or use the view buttons in the menu bar. The topic is shown as blocks rather than as tags.\n3. **Add a short description** Put the cursor after the title and use the insert menu to add a `shortdesc`, then write one sentence that says what the topic is for.\n4. **Save** Select **File > Save**. The XML is written back with your formatting intact.\n:::\n\nNow switch back with **View > Document Views > Editor** and confirm the element is where you expect. The two views are the same document, and your place in it is carried across.\n\n## Step 4: Let the agent do the rest {#step-4-let-the-agent-do-the-rest}\n\nFixing thirty topics by hand is the wrong tool. This is what the agent is for.\n\n:::steps\n1. **Open the AI Agent panel** It is on the right-hand sidebar.\n2. **Choose a provider and sign in** Use the provider bar at the top of the panel. The key is stored in your operating system keychain, not in the project.\n3. **Ask for the change** Type: `Add a short description to every topic that has none. Keep them to one sentence and lead with what the topic is for.`\n:::\n\nThe agent works across the project rather than opening each file in turn, because it has the editor's project operations available to it.\n\n## Step 5: Review what it proposes {#step-5-review-what-it-proposes}\n\nThe agent's edits do not overwrite your files. They arrive as tracked changes.\n\n:::steps\n1. **Open the Proposals panel** Each proposed change is listed with the identity that made it.\n2. **Select one** The editor jumps to the affected part of the document and shows the change in place.\n3. **Accept or reject it** Use **Accept** or **Reject** for one change, or **Accept all** and **Reject all** when you have read enough to trust the batch.\n:::\n\nThis is the part worth slowing down for. The agent proposes; you decide.\n\n## Step 6: Check the project again {#step-6-check-the-project-again}\n\n```bash\nbin/dogsbay-xml project-health .\n```\n\nFewer problems should remain. What is left is the more interesting work: the hardcoded product name that should be a key, the conditions that are not in the subject scheme, and the topic that is too long and should be split.\n\n## What you learned {#what-you-learned}\n\n- The sample project is a copy, so you can experiment freely.\n- Problems are found at project level, not file by file.\n- The Author view and the source view are two views of one document.\n- The agent works across a project, and its changes are reviewable before they land.\n\n## Where to go next {#where-to-go-next}\n\n:::cards\n- **[The agent, and what it may touch](/dogsbay-xml-docs/agent/overview){icon=\"sparkles\"}**\n  Tiers, the write gate, and the audit log.\n\n- **[Reviewing an agent's changes](/dogsbay-xml-docs/agent/proposals){icon=\"check\"}**\n  Proposals in more detail, including comments.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/start-here/tutorial.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="tutorial-fixing-a-dita-guide" class="group scroll-mt-20">Tutorial: fixing a DITA guide<a href="#tutorial-fixing-a-dita-guide" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>In this tutorial you inherit a documentation set that does not build cleanly, find out what is wrong with it, and fix part of it with the AI agent.</p>
      <p>The project is a DITA user guide for the Audacity audio editor. It ships with the editor, and it is deliberately broken: topics without descriptions, links that point nowhere, a hardcoded product name that should be a key, and conditions that no subject scheme allows.</p>
      <p><strong>Time:</strong> about 30 minutes. <strong>You need:</strong> DogsBay XML installed. See <a href="./install">Installing</a>. An API key for the agent section is useful but not required until then.</p>
      <h2 id="step-1-open-the-sample-project" class="group scroll-mt-20">Step 1: Open the sample project<a href="#step-1-open-the-sample-project" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Steps>
      <Step>
      <strong>Select File &gt; Open Sample Project</strong> The editor asks where to put it.
      </Step>
      <Step>
      <strong>Choose a location</strong> The sample is copied into a new <code>audacity-demo</code> folder inside the location you choose. It never uses the folder you chose as the project itself, and it never overwrites anything: if <code>audacity-demo</code> exists, the copy becomes <code>audacity-demo-2</code>.
      </Step>
      <Step>
      <strong>Wait for the project to open</strong> The Map Explorer loads <code>audacity-guide.ditamap</code>, and publishing is configured, without you setting anything up.
      </Step>
      </Steps>
      <p>You now have your own copy to break further and repair.</p>
      <h2 id="step-2-see-what-is-wrong" class="group scroll-mt-20">Step 2: See what is wrong<a href="#step-2-see-what-is-wrong" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Rather than opening files to look for problems, ask the project.</p>
      <p>Run the health check from the command line:</p>
      <CodeBlock code={"bin/dogsbay-xml project-health ."} lang="bash" />
      <p>The report covers three kinds of problem at once: references that point at something missing, keys that are used but never defined or defined but never used, topics that no map includes, and files that fail validation against their grammar.</p>
      <p>Read the output before you change anything. The point of this step is that the answer is a property of the project, not of the file you happen to have open.</p>
      <Alert variant="tip" class="my-4">
        <AlertTitle>Tip</AlertTitle>
        <AlertDescription>
          <p><code>check-links</code> and <code>health</code> are narrower and faster if you only want one part of that picture.</p>
        </AlertDescription>
      </Alert>
      <h2 id="step-3-fix-one-problem-by-hand" class="group scroll-mt-20">Step 3: Fix one problem by hand<a href="#step-3-fix-one-problem-by-hand" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Pick a topic that has no short description.</p>
      <Steps>
      <Step>
      <strong>Open the topic</strong> Select it in the Explorer.
      </Step>
      <Step>
      <strong>Switch to the Author view</strong> Select <strong>View &gt; Document Views &gt; Author</strong>, or use the view buttons in the menu bar. The topic is shown as blocks rather than as tags.
      </Step>
      <Step>
      <strong>Add a short description</strong> Put the cursor after the title and use the insert menu to add a <code>shortdesc</code>, then write one sentence that says what the topic is for.
      </Step>
      <Step>
      <strong>Save</strong> Select <strong>File &gt; Save</strong>. The XML is written back with your formatting intact.
      </Step>
      </Steps>
      <p>Now switch back with <strong>View &gt; Document Views &gt; Editor</strong> and confirm the element is where you expect. The two views are the same document, and your place in it is carried across.</p>
      <h2 id="step-4-let-the-agent-do-the-rest" class="group scroll-mt-20">Step 4: Let the agent do the rest<a href="#step-4-let-the-agent-do-the-rest" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Fixing thirty topics by hand is the wrong tool. This is what the agent is for.</p>
      <Steps>
      <Step>
      <strong>Open the AI Agent panel</strong> It is on the right-hand sidebar.
      </Step>
      <Step>
      <strong>Choose a provider and sign in</strong> Use the provider bar at the top of the panel. The key is stored in your operating system keychain, not in the project.
      </Step>
      <Step>
      <strong>Ask for the change</strong> Type: <code>Add a short description to every topic that has none. Keep them to one sentence and lead with what the topic is for.</code>
      </Step>
      </Steps>
      <p>The agent works across the project rather than opening each file in turn, because it has the editor's project operations available to it.</p>
      <h2 id="step-5-review-what-it-proposes" class="group scroll-mt-20">Step 5: Review what it proposes<a href="#step-5-review-what-it-proposes" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The agent's edits do not overwrite your files. They arrive as tracked changes.</p>
      <Steps>
      <Step>
      <strong>Open the Proposals panel</strong> Each proposed change is listed with the identity that made it.
      </Step>
      <Step>
      <strong>Select one</strong> The editor jumps to the affected part of the document and shows the change in place.
      </Step>
      <Step>
      <strong>Accept or reject it</strong> Use <strong>Accept</strong> or <strong>Reject</strong> for one change, or <strong>Accept all</strong> and <strong>Reject all</strong> when you have read enough to trust the batch.
      </Step>
      </Steps>
      <p>This is the part worth slowing down for. The agent proposes; you decide.</p>
      <h2 id="step-6-check-the-project-again" class="group scroll-mt-20">Step 6: Check the project again<a href="#step-6-check-the-project-again" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <CodeBlock code={"bin/dogsbay-xml project-health ."} lang="bash" />
      <p>Fewer problems should remain. What is left is the more interesting work: the hardcoded product name that should be a key, the conditions that are not in the subject scheme, and the topic that is too long and should be split.</p>
      <h2 id="what-you-learned" class="group scroll-mt-20">What you learned<a href="#what-you-learned" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <ul>
      <li>The sample project is a copy, so you can experiment freely.</li>
      <li>Problems are found at project level, not file by file.</li>
      <li>The Author view and the source view are two views of one document.</li>
      <li>The agent works across a project, and its changes are reviewable before they land.</li>
      </ul>
      <h2 id="where-to-go-next" class="group scroll-mt-20">Where to go next<a href="#where-to-go-next" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/agent/overview" class="[text-decoration:none] text-foreground">
      <Icon name="sparkles" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">The agent, and what it may touch</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Tiers, the write gate, and the audit log.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="/dogsbay-xml-docs/agent/proposals" class="[text-decoration:none] text-foreground">
      <Icon name="check" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Reviewing an agent's changes</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Proposals in more detail, including comments.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

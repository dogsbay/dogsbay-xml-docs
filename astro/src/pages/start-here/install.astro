---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: start-here/install.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Alert from "@ui/alert/Alert.astro";
import AlertDescription from "@ui/alert/AlertDescription.astro";
import AlertTitle from "@ui/alert/AlertTitle.astro";
import CodeBlock from "@ui/code-block/CodeBlock.astro";
import Step from "@ui/steps/Step.astro";
import Steps from "@ui/steps/Steps.astro";
import Table from "@ui/table/Table.astro";
import TableBody from "@ui/table/TableBody.astro";
import TableCell from "@ui/table/TableCell.astro";
import TableHead from "@ui/table/TableHead.astro";
import TableHeader from "@ui/table/TableHeader.astro";
import TableRow from "@ui/table/TableRow.astro";

const headings = [{"depth":1,"slug":"installing","text":"Installing"},{"depth":2,"slug":"installing-from-a-package","text":"Installing from a package"},{"depth":2,"slug":"building-from-source","text":"Building from source"},{"depth":2,"slug":"enabling-the-command-line","text":"Enabling the command line"},{"depth":2,"slug":"turning-on-the-integration-server","text":"Turning on the integration server"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/start-here/install";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Installing";
const description = "Install DogsBay XML from a packaged installer, or build it from source." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "how-to";
const pageAudience = null;
const pageCategory = ["start-here"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Installing {#installing}\n\nDogsBay XML runs on Linux, macOS and Windows.\n\n## Installing from a package {#installing-from-a-package}\n\nEach installer bundles its own Java runtime, so no separate JDK is required.\n\n:::steps\n1. **Download the installer for your platform** Go to the [releases page](https://github.com/dogsbay/dogsbay-xml/releases) and download the `.deb` or `.rpm` package for Linux, the `.dmg` for macOS, or the Windows installer.\n2. **Install it the usual way for your platform** On Linux, install the package with your package manager. On macOS, open the disk image and drag the application to Applications. On Windows, run the installer.\n3. **Start the editor** The Welcome tab opens. From there you can open the sample project, which is where the [tutorial](./tutorial) starts.\n:::\n\n## Building from source {#building-from-source}\n\nBuilding requires Java 25.\n\n```bash\ngit clone https://github.com/dogsbay/dogsbay-xml\ncd dogsbay-xml\n./gradlew run\n```\n\nOther tasks:\n\n| Task | Result |\n| --- | --- |\n| `./gradlew run` | Start the editor |\n| `./gradlew test` | Run the test suite |\n| `./gradlew shadowJar` | Build a self-contained JAR |\n| `./gradlew jpackage` | Build a native installer for the current platform |\n\n## Enabling the command line {#enabling-the-command-line}\n\nThe `bin/dogsbay-xml` command needs its libraries copied into place once before first use:\n\n```bash\n./gradlew syncLib\nbin/dogsbay-xml --help\n```\n\nSome commands work entirely on files and need nothing else. Commands that drive a running editor, such as `open` or `screenshot`, also need the integration server.\n\n## Turning on the integration server {#turning-on-the-integration-server}\n\nThe integration server is what AI assistants and the live-editor commands connect to. It is off until you turn it on.\n\n:::steps\n1. **Open the preferences** Select **File > Preferences**.\n2. **Turn the server on** Go to the **Server** page and enable the server.\n3. **Check that it is listening** Run `bin/dogsbay-xml status`.\n:::\n\n> [!NOTE]\n> The built-in AI agent does not need the integration server. It calls the editor's operations directly, inside the same process.\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/start-here/install.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="installing" class="group scroll-mt-20">Installing<a href="#installing" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>DogsBay XML runs on Linux, macOS and Windows.</p>
      <h2 id="installing-from-a-package" class="group scroll-mt-20">Installing from a package<a href="#installing-from-a-package" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Each installer bundles its own Java runtime, so no separate JDK is required.</p>
      <Steps>
      <Step>
      <strong>Download the installer for your platform</strong> Go to the <a href="https://github.com/dogsbay/dogsbay-xml/releases">releases page</a> and download the <code>.deb</code> or <code>.rpm</code> package for Linux, the <code>.dmg</code> for macOS, or the Windows installer.
      </Step>
      <Step>
      <strong>Install it the usual way for your platform</strong> On Linux, install the package with your package manager. On macOS, open the disk image and drag the application to Applications. On Windows, run the installer.
      </Step>
      <Step>
      <strong>Start the editor</strong> The Welcome tab opens. From there you can open the sample project, which is where the <a href="./tutorial">tutorial</a> starts.
      </Step>
      </Steps>
      <h2 id="building-from-source" class="group scroll-mt-20">Building from source<a href="#building-from-source" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Building requires Java 25.</p>
      <CodeBlock code={"git clone https://github.com/dogsbay/dogsbay-xml\ncd dogsbay-xml\n./gradlew run"} lang="bash" />
      <p>Other tasks:</p>
      <Table class="my-4">
      <TableHeader>
      <TableRow>
      <TableHead>Task</TableHead>
      <TableHead>Result</TableHead>
      </TableRow>
      </TableHeader>
      <TableBody>
      <TableRow>
      <TableCell><code>./gradlew run</code></TableCell>
      <TableCell>Start the editor</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>./gradlew test</code></TableCell>
      <TableCell>Run the test suite</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>./gradlew shadowJar</code></TableCell>
      <TableCell>Build a self-contained JAR</TableCell>
      </TableRow>
      <TableRow>
      <TableCell><code>./gradlew jpackage</code></TableCell>
      <TableCell>Build a native installer for the current platform</TableCell>
      </TableRow>
      </TableBody>
      </Table>
      <h2 id="enabling-the-command-line" class="group scroll-mt-20">Enabling the command line<a href="#enabling-the-command-line" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The <code>bin/dogsbay-xml</code> command needs its libraries copied into place once before first use:</p>
      <CodeBlock code={"./gradlew syncLib\nbin/dogsbay-xml --help"} lang="bash" />
      <p>Some commands work entirely on files and need nothing else. Commands that drive a running editor, such as <code>open</code> or <code>screenshot</code>, also need the integration server.</p>
      <h2 id="turning-on-the-integration-server" class="group scroll-mt-20">Turning on the integration server<a href="#turning-on-the-integration-server" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>The integration server is what AI assistants and the live-editor commands connect to. It is off until you turn it on.</p>
      <Steps>
      <Step>
      <strong>Open the preferences</strong> Select <strong>File &gt; Preferences</strong>.
      </Step>
      <Step>
      <strong>Turn the server on</strong> Go to the <strong>Server</strong> page and enable the server.
      </Step>
      <Step>
      <strong>Check that it is listening</strong> Run <code>bin/dogsbay-xml status</code>.
      </Step>
      </Steps>
      <Alert variant="note" class="my-4">
        <AlertTitle>Note</AlertTitle>
        <AlertDescription>
          <p>The built-in AI agent does not need the integration server. It calls the editor's operations directly, inside the same process.</p>
        </AlertDescription>
      </Alert>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

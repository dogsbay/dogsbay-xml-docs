---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: getting-started.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import CodeBlock from "@ui/code-block/CodeBlock.astro";

const headings = [{"depth":1,"slug":"getting-started","text":"Getting started"},{"depth":2,"slug":"1-edit-a-page","text":"1. Edit a page"},{"depth":2,"slug":"2-add-a-page","text":"2. Add a page"},{"depth":2,"slug":"3-group-pages","text":"3. Group pages"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/getting-started";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Getting started";
const description = "Quick orientation for new contributors." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = null;
const pageAudience = null;
const pageCategory = null;
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Getting started {#getting-started}\n\nThree minutes to your first edit. Powered by [Dogsbay](https://github.com/dogsbay/dogsbay).\n\n## 1. Edit a page {#1-edit-a-page}\n\nOpen `content/index.md` in your editor and change something. Save — the dev server reloads automatically.\n\n## 2. Add a page {#2-add-a-page}\n\nCreate a new file like `content/about.md`:\n\n```md\n---\ntitle: About\n---\n\n# About\n\nWhatever you want to say here.\n```\n\nThen add it to `content/nav.yml` so it appears in the sidebar. Each entry is a single-key map — key = label, value = file path:\n\n```yaml\n- About: about.md\n```\n\nExternal URLs work the same way:\n\n```yaml\n- GitHub: https://github.com/your-org/your-repo\n```\n\n## 3. Group pages {#3-group-pages}\n\nTo create a section in the sidebar, give the entry a list of children instead of a single file:\n\n```yaml\n- Guides:\n    - Configuration: guides/configuration.md\n    - Deployment: guides/deployment.md\n```\n\nThe folder structure under `content/` doesn't have to match the nav — but it usually does, because it makes URLs predictable.\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/getting-started.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={false}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="getting-started" class="group scroll-mt-20">Getting started<a href="#getting-started" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>Three minutes to your first edit. Powered by <a href="https://github.com/dogsbay/dogsbay">Dogsbay</a>.</p>
      <h2 id="1-edit-a-page" class="group scroll-mt-20">1. Edit a page<a href="#1-edit-a-page" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Open <code>content/index.md</code> in your editor and change something. Save — the dev server reloads automatically.</p>
      <h2 id="2-add-a-page" class="group scroll-mt-20">2. Add a page<a href="#2-add-a-page" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>Create a new file like <code>content/about.md</code>:</p>
      <CodeBlock code={"---\ntitle: About\n---\n\n# About\n\nWhatever you want to say here."} lang="md" />
      <p>Then add it to <code>content/nav.yml</code> so it appears in the sidebar. Each entry is a single-key map — key = label, value = file path:</p>
      <CodeBlock code={"- About: about.md"} lang="yaml" />
      <p>External URLs work the same way:</p>
      <CodeBlock code={"- GitHub: https://github.com/your-org/your-repo"} lang="yaml" />
      <h2 id="3-group-pages" class="group scroll-mt-20">3. Group pages<a href="#3-group-pages" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <p>To create a section in the sidebar, give the entry a list of children instead of a single file:</p>
      <CodeBlock code={"- Guides:\n    - Configuration: guides/configuration.md\n    - Deployment: guides/deployment.md"} lang="yaml" />
      <p>The folder structure under <code>content/</code> doesn't have to match the nav — but it usually does, because it makes URLs predictable.</p>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

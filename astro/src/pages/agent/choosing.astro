---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: agent/choosing.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";

const headings = [{"depth":1,"slug":"which-agent-to-use","text":"Which agent to use"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/agent/choosing";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Which agent to use";
const description = "Compare the built-in and hosted agents and learn when to run both." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "explanation";
const pageAudience = null;
const pageCategory = ["agent"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Which agent to use {#which-agent-to-use}\n\nDogsBay XML runs two kinds of agent for different kinds of work. The built-in agent knows your project. A hosted agent is one that you already use. Neither is a lesser version of the other.\n\nUse the **built-in agent** when you want an agent that is already set up and already knows where you are.\n\n- **Nothing else to install or enable.** It runs inside the editor, so you do not need to turn on the integration server or install another program. A hosted agent needs both.\n- **It knows the document you are looking at.** Every turn includes the active file, its type and grammar, the active deliverable and its root map, and the documents you have open. A hosted agent must use tool calls to discover that context. Without it, the agent might respond to \"add a shortdesc to this topic\" by asking which topic you mean.\n- **It integrates with the editor.** Drag a file onto the chat to attach it, open documents reload when a turn changes them on disk, and `/revert` undoes the file changes of the last turn from a checkpoint taken before it ran. The editor stores those copies in `.xagent/checkpoints`, replaces them at the start of each turn, and excludes them from version control. A hosted agent has no equivalent. To undo its work, use git or the agent's own undo.\n- **You choose the model per conversation.** `/provider` and `/model` switch between Anthropic, OpenAI, Gemini, ChatGPT, and Ollama without leaving the panel.\n- **The model can run on your machine.** With Ollama as the provider, your content is not sent to a model provider: inference happens locally. That is not the same as nothing leaving the machine. The agent can run shell commands, and any MCP servers or skills you configure run with it, so a tool you have given it can still reach a network. For a documentation set under a nondisclosure agreement (NDA), choosing Ollama settles where the model runs; what the agent is allowed to do is settled by the [capability tier](/dogsbay-xml-docs/agent/overview#capability-tier) and by which tools you configure.\n\nUse a **hosted agent** when you want its own workflow and features.\n\n- **You already use it and pay for it.** Claude Code, Codex, and Gemini CLI use their own sign-in, so your existing subscription applies.\n- **You want that agent's features.** You can use its planning, file tools, and approach to large refactoring tasks.\n- **You want to limit its editor access.** A hosted session starts at T1, and you decide which tools to provide. The built-in agent runs with your authority. See [Capability tier](./overview#capability-tier). An agent with its own file tools effectively operates at Tier 3 because the tier limits only what the editor provides.\n- **You can also use a local model.** If the agent supports local models, configure it to use one. For example, OpenCode supports an OpenAI-compatible provider that points to Ollama, LM Studio, or llama.cpp, and Goose supports Ollama. Configure the model in the agent, not the editor.\n\nYou can use both kinds of agent. For example, use the built-in agent for DITA work, where the map, keys, and deliverable provide essential context. Use a hosted agent alongside it for code-related work.\n\n## Related {#related}\n\n:::cards\n- **[The agent, and what it may touch](./overview){icon=\"shield\"}**\n  Tiers, the write gate, and the audit log, which apply to both.\n\n- **[Using Claude Code, Codex, or Gemini](./hosted-agents){icon=\"terminal\"}**\n  Starting a hosted agent, signing it in, and what it needs.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/agent/choosing.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  pagefindBaseUrl={"/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="which-agent-to-use" class="group scroll-mt-20">Which agent to use<a href="#which-agent-to-use" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h1>
      <p>DogsBay XML runs two kinds of agent for different kinds of work. The built-in agent knows your project. A hosted agent is one that you already use. Neither is a lesser version of the other.</p>
      <p>Use the <strong>built-in agent</strong> when you want an agent that is already set up and already knows where you are.</p>
      <ul>
      <li><strong>Nothing else to install or enable.</strong> It runs inside the editor, so you do not need to turn on the integration server or install another program. A hosted agent needs both.</li>
      <li><strong>It knows the document you are looking at.</strong> Every turn includes the active file, its type and grammar, the active deliverable and its root map, and the documents you have open. A hosted agent must use tool calls to discover that context. Without it, the agent might respond to "add a shortdesc to this topic" by asking which topic you mean.</li>
      <li><strong>It integrates with the editor.</strong> Drag a file onto the chat to attach it, open documents reload when a turn changes them on disk, and <code>/revert</code> undoes the file changes of the last turn from a checkpoint taken before it ran. The editor stores those copies in <code>.xagent/checkpoints</code>, replaces them at the start of each turn, and excludes them from version control. A hosted agent has no equivalent. To undo its work, use git or the agent's own undo.</li>
      <li><strong>You choose the model per conversation.</strong> <code>/provider</code> and <code>/model</code> switch between Anthropic, OpenAI, Gemini, ChatGPT, and Ollama without leaving the panel.</li>
      <li><strong>The model can run on your machine.</strong> With Ollama as the provider, your content is not sent to a model provider: inference happens locally. That is not the same as nothing leaving the machine. The agent can run shell commands, and any MCP servers or skills you configure run with it, so a tool you have given it can still reach a network. For a documentation set under a nondisclosure agreement (NDA), choosing Ollama settles where the model runs; what the agent is allowed to do is settled by the <a href="/dogsbay-xml-docs/agent/overview#capability-tier">capability tier</a> and by which tools you configure.</li>
      </ul>
      <p>Use a <strong>hosted agent</strong> when you want its own workflow and features.</p>
      <ul>
      <li><strong>You already use it and pay for it.</strong> Claude Code, Codex, and Gemini CLI use their own sign-in, so your existing subscription applies.</li>
      <li><strong>You want that agent's features.</strong> You can use its planning, file tools, and approach to large refactoring tasks.</li>
      <li><strong>You want to limit its editor access.</strong> A hosted session starts at T1, and you decide which tools to provide. The built-in agent runs with your authority. See <a href="./overview#capability-tier">Capability tier</a>. An agent with its own file tools effectively operates at Tier 3 because the tier limits only what the editor provides.</li>
      <li><strong>You can also use a local model.</strong> If the agent supports local models, configure it to use one. For example, OpenCode supports an OpenAI-compatible provider that points to Ollama, LM Studio, or llama.cpp, and Goose supports Ollama. Configure the model in the agent, not the editor.</li>
      </ul>
      <p>You can use both kinds of agent. For example, use the built-in agent for DITA work, where the map, keys, and deliverable provide essential context. Use a hosted agent alongside it for code-related work.</p>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1" data-pagefind-ignore>&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./overview" class="[text-decoration:none] text-foreground">
      <Icon name="shield" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">The agent, and what it may touch</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Tiers, the write gate, and the audit log, which apply to both.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./hosted-agents" class="[text-decoration:none] text-foreground">
      <Icon name="terminal" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Using Claude Code, Codex, or Gemini</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Starting a hosted agent, signing it in, and what it needs.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

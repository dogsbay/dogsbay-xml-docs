---
// AUTO-GENERATED by `dogsbay convert` — do not edit.
// Source: agent/choosing.md
// Edit the source markdown and re-run `dogsbay convert` to regenerate.

import "@/styles/global.css";
import "katex/dist/katex.min.css";
import DocsLayout from "@dogsbay/docs-layout/DocsLayout.astro";
import { getPagination } from "@dogsbay/docs-layout/pagination";
import { filterNavByAxis } from "@dogsbay/docs-layout/nav-filter";
import type { SiteConfig } from "@dogsbay/types";
import navData from "@/data/nav.json";
import siteConfigData from "@/data/site.json";
import switcherMapData from "@/data/switcherMap.json";
import MarkdownContentStack from "@/components/wrappers/MarkdownContentStack.astro";
import RightRailStack from "@/components/wrappers/RightRailStack.astro";
const siteConfig = siteConfigData as SiteConfig;
import Card from "@ui/card/Card.astro";
import CardContent from "@ui/card/CardContent.astro";
import Grid from "@ui/grid/Grid.astro";
import GridItem from "@ui/grid/GridItem.astro";
import Icon from "@ui/icon/Icon.astro";

const headings = [{"depth":1,"slug":"which-agent-to-use","text":"Which agent to use"},{"depth":2,"slug":"related","text":"Related"}];
const nav = navData;
const currentPath = "/dogsbay-xml-docs/agent/choosing";
const _navForPagination = filterNavByAxis(nav as any[], {
  basePath: "",
  namespace: null ?? undefined,
  version: null ?? undefined,
  locale: null ?? undefined,
});
const { prev, next } = getPagination(currentPath, _navForPagination);
const title = "Which agent to use";
const description = "What the built-in agent is good at, what a hosted agent is good at, and why running both is a reasonable answer." || undefined;
const ogImage = "" || undefined;
const noindex = false;
const nofollow = false;
const excludeFromSearch = false;
const pageTags = null;
const pageStatus = null;
const pageType = "explanation";
const pageAudience = null;
const pageCategory = ["agent"];
const pageTaxonomies = null;
const tagsIndexPath = "/dogsbay-xml-docs/tags";
const lastUpdated = null || undefined;
const llmActionsProps = {"enabled":true,"providers":["claude","chatgpt","perplexity","gemini"],"placement":"header","copyButton":true,"footerLink":true,"markdownBody":"# Which agent to use {#which-agent-to-use}\n\nDogsBay XML runs two kinds of agent, and they are good at different things. The built-in agent knows your project; a hosted agent is the one you already use. Neither is a lesser version of the other.\n\nUse the **built-in agent** when you want an agent that is already set up and already knows where you are.\n\n- **Nothing else to install or enable.** It runs inside the editor, so there is no integration server to turn on and no other program to install. A hosted agent needs both.\n- **It knows the document you are looking at.** Every turn carries the active file, its type and grammar, the active deliverable and its root map, and the documents you have open. A hosted agent has to spend tool calls discovering that, and often does not, which is the difference between \"add a shortdesc to this topic\" working immediately and being asked which topic you mean.\n- **It is wired into the editor.** Drag a file onto the chat to attach it, open documents reload when a turn changes them on disk, and `/revert` undoes the file changes of the last turn from a checkpoint taken before it ran.\n- **You choose the model per conversation.** `/provider` and `/model` switch between Anthropic, OpenAI, Gemini, ChatGPT and Ollama without leaving the panel.\n- **It can run entirely on your machine.** With Ollama as the provider, nothing leaves the machine — which is sometimes the only acceptable answer for a documentation set under NDA.\n\nUse a **hosted agent** when the agent itself is the reason.\n\n- **You already use it, and already pay for it.** Claude Code, Codex and Gemini CLI bring their own sign-in, so your existing subscription applies.\n- **You want that agent's harness**: its planning, its own file tools, its way of working through a large refactor.\n- **You want a ceiling on it.** A hosted session starts at T1 and you decide what to lend it, where the built-in agent runs with your own authority. See [capability tier](./overview#capability-tier), and note the caveat there: an agent with its own file tools is Tier 3 by construction, and the tier limits only what the editor lends it.\n- **Local is possible here too**, if the agent supports it: OpenCode takes an OpenAI-compatible provider pointed at Ollama, LM Studio or llama.cpp, and Goose has Ollama as a provider of its own. That is the agent's own configuration, not something the editor sets.\n\nThe two are not exclusive. The built-in agent is the first tab and a hosted agent gets its own, so a common arrangement is the built-in agent for the DITA work — where knowing the map, the keys and the deliverable is what makes the answer right — and a hosted agent alongside it for the work that is really about code.\n\n## Related {#related}\n\n:::cards\n- **[The agent, and what it may touch](./overview){icon=\"shield\"}**\n  Tiers, the write gate and the audit log, which apply to both.\n\n- **[Using Claude Code, Codex or Gemini](./hosted-agents){icon=\"terminal\"}**\n  Starting a hosted agent, signing it in, and what it needs.\n:::\n","mdUrl":"https://dogsbay.ai/dogsbay-xml-docs/agent/choosing.md"} ?? undefined;
---

<DocsLayout
  siteName={siteConfig.siteName}
  title={title}
  nav={nav as any[]}
  headings={headings}
  prev={prev}
  next={next}
  description={description}
  siteDescription={siteConfig.description || undefined}
  ogImage={ogImage}
  defaultOgImage={siteConfig.ogImage || undefined}
  siteUrl={siteConfig.siteUrl || "/"}
  twitterHandle={siteConfig.twitterHandle || undefined}
  themeColor={siteConfig.themeColor || undefined}
  noindex={noindex}
  nofollow={nofollow}
  excludeFromSearch={excludeFromSearch}
  plausibleDomain={siteConfig.plausible?.domain}
  plausibleScriptUrl={siteConfig.plausible?.scriptUrl}
  repoUrl={siteConfig.repoUrl || undefined}
  mdMirror={true}
  tags={pageTags ?? undefined}
  tagsIndexPath={tagsIndexPath}
  tagPrefixes={siteConfig.tagPrefixes}
  tagLabels={siteConfig.tagLabels}
  taxonomyIndexPaths={siteConfig.taxonomyIndexPaths}
  taxonomyDisplay={siteConfig.taxonomyDisplay}
  status={pageStatus ?? undefined}
  pageType={pageType ?? undefined}
  audience={pageAudience ?? undefined}
  category={pageCategory ?? undefined}
  lastUpdated={lastUpdated}
  taxonomies={pageTaxonomies ?? undefined}
  autoH1={false}
  autoLede={false}
  llmActions={llmActionsProps}
  linkIcons={siteConfig.linkIcons}
  poweredBy={siteConfig.poweredBy !== false}
  multiSource={null ?? undefined}
  switcherMap={switcherMapData}
  basePath={"/dogsbay-xml-docs"}
  navMode={"client"}
  pagefindUrl={"/dogsbay-xml-docs/pagefind/"}
  favicon={"/dogsbay-xml-docs/favicon.svg"}
  wideLayout={false}
  toc={"top"}
>
  <RightRailStack slot="right-rail" />
  <MarkdownContentStack>
    <article class="docs-prose">
      <h1 id="which-agent-to-use" class="group scroll-mt-20">Which agent to use<a href="#which-agent-to-use" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h1>
      <p>DogsBay XML runs two kinds of agent, and they are good at different things. The built-in agent knows your project; a hosted agent is the one you already use. Neither is a lesser version of the other.</p>
      <p>Use the <strong>built-in agent</strong> when you want an agent that is already set up and already knows where you are.</p>
      <ul>
      <li><strong>Nothing else to install or enable.</strong> It runs inside the editor, so there is no integration server to turn on and no other program to install. A hosted agent needs both.</li>
      <li><strong>It knows the document you are looking at.</strong> Every turn carries the active file, its type and grammar, the active deliverable and its root map, and the documents you have open. A hosted agent has to spend tool calls discovering that, and often does not, which is the difference between "add a shortdesc to this topic" working immediately and being asked which topic you mean.</li>
      <li><strong>It is wired into the editor.</strong> Drag a file onto the chat to attach it, open documents reload when a turn changes them on disk, and <code>/revert</code> undoes the file changes of the last turn from a checkpoint taken before it ran.</li>
      <li><strong>You choose the model per conversation.</strong> <code>/provider</code> and <code>/model</code> switch between Anthropic, OpenAI, Gemini, ChatGPT and Ollama without leaving the panel.</li>
      <li><strong>It can run entirely on your machine.</strong> With Ollama as the provider, nothing leaves the machine — which is sometimes the only acceptable answer for a documentation set under NDA.</li>
      </ul>
      <p>Use a <strong>hosted agent</strong> when the agent itself is the reason.</p>
      <ul>
      <li><strong>You already use it, and already pay for it.</strong> Claude Code, Codex and Gemini CLI bring their own sign-in, so your existing subscription applies.</li>
      <li><strong>You want that agent's harness</strong>: its planning, its own file tools, its way of working through a large refactor.</li>
      <li><strong>You want a ceiling on it.</strong> A hosted session starts at T1 and you decide what to lend it, where the built-in agent runs with your own authority. See <a href="./overview#capability-tier">capability tier</a>, and note the caveat there: an agent with its own file tools is Tier 3 by construction, and the tier limits only what the editor lends it.</li>
      <li><strong>Local is possible here too</strong>, if the agent supports it: OpenCode takes an OpenAI-compatible provider pointed at Ollama, LM Studio or llama.cpp, and Goose has Ollama as a provider of its own. That is the agent's own configuration, not something the editor sets.</li>
      </ul>
      <p>The two are not exclusive. The built-in agent is the first tab and a hosted agent gets its own, so a common arrangement is the built-in agent for the DITA work — where knowing the map, the keys and the deliverable is what makes the answer right — and a hosted agent alongside it for the work that is really about code.</p>
      <h2 id="related" class="group scroll-mt-20">Related<a href="#related" class="ml-2 text-muted-foreground opacity-0 group-hover:opacity-100 no-underline" aria-hidden="true" tabindex="-1">&para;</a></h2>
      <Grid cols={[1, 2]} gap={4}>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./overview" class="[text-decoration:none] text-foreground">
      <Icon name="shield" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">The agent, and what it may touch</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Tiers, the write gate and the audit log, which apply to both.</p>
      </a>
      </CardContent>
      </Card>
      <Card class="p-4 hover:border-foreground/20 transition-colors">
      <CardContent class="p-0">
      <a href="./hosted-agents" class="[text-decoration:none] text-foreground">
      <Icon name="terminal" class="size-5 mb-2 text-muted-foreground" />
      <h3 class="text-base font-semibold">Using Claude Code, Codex or Gemini</h3>
      <p class="text-sm text-muted-foreground mt-1 [text-decoration:none]">Starting a hosted agent, signing it in, and what it needs.</p>
      </a>
      </CardContent>
      </Card>
      </Grid>
    </article>
  </MarkdownContentStack>
</DocsLayout>

<script>
  // @ts-ignore — generated by dogsbay site build
  import "/src/lib/plugin-runtime.ts";
</script>

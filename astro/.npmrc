# Load-bearing. Under pnpm's default isolated layout the Astro build fails
# with "Rolldown failed to resolve import @astrojs/internal-helpers/path",
# even though the package is installed inside astro's own node_modules.
node-linker=hoisted
